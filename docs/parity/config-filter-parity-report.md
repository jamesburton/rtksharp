# `rtk config` / TOML Filter / `rtk trust` Oracle Parity Report

> Auto-generated by `RtkSharp.ParityTests/ConfigFilterParityTests.cs` (Phase 4 Task 6 acceptance gate). Do not edit by hand — re-run the test to refresh.

- **Generated (UTC):** 2026-07-04 16:30:29
- **Platform:** Windows
- **Oracle:** `C:\Development\rtksharp\target\release\rtk.exe`
- **RtkSharp:** `C:\Development\rtksharp\RtkSharp.ParityTests\bin\Release\net10.0\RtkSharp.exe`
- **Battery size:** 18 entries (4 `rtk config` + 1 TOML built-in fallback dispatch + 8 `rtk trust`/`rtk untrust` + 5 `rtk verify`)

Comparison method: each entry runs both binaries in a fresh, isolated temp directory with `StdinContent = ""` (forced non-interactive stdin). Every entry redirects both real, non-env-redirectable-on-Windows directories (`%APPDATA%\rtk` config-dir and `%LOCALAPPDATA%\rtk` trust-store/tracking data-dir) via an NTFS junction (`HostRealDirGuard`, this file's generalized sibling of `InitParityTests.WindowsGlobalFiltersGuard`) before the oracle ever runs — applied unconditionally to every entry, since the oracle writes usage-tracking data on every invocation regardless of subcommand (see this file's class remarks). If either real directory already has content on the host, the entry is skipped entirely rather than risking any read/write against it. Multi-step entries compare only the final step's stdout (CRLF/LF normalized, temp-path masked) and exit code. The `config --create` entries additionally compare a full relative-path -> SHA-256 manifest of the redirected config directory after all steps (with the oracle's plan-deferred `[tee]` section masked out of the comparison — see `docs/parity/compatibility-ledger.md`).

## Summary

| Metric | Result |
|--------|--------|
| **Overall parity (headline)** | **100.0%** |
| Threshold | 95% |
| Entries matched | 18 / 18 |

## Per-entry results

| Entry | Rust exit | .NET exit | Stdout match | Tree match | Verdict |
|-------|-----------|-----------|--------------|------------|---------|
| `config: no file (defaults shown)` | 0 | 0 | yes | yes | SKIPPED (host-safety) |
| `config: pre-existing file (shows created content)` | 0 | 0 | yes | yes | SKIPPED (host-safety) |
| `config --create` | 0 | 0 | yes | yes | SKIPPED (host-safety) |
| `config --create (idempotent re-run)` | 0 | 0 | yes | yes | SKIPPED (host-safety) |
| `TOML fallback dispatch: built-in df filter` | 0 | 0 | yes | yes | SKIPPED (host-safety) |
| `trust: no .rtk/filters.toml present (fail-loud)` | 0 | 0 | yes | yes | SKIPPED (host-safety) |
| `trust --list: empty store` | 0 | 0 | yes | yes | SKIPPED (host-safety) |
| `trust: happy path (dump + risk summary + trusted)` | 0 | 0 | yes | yes | SKIPPED (host-safety) |
| `trust --list: after trusting` | 0 | 0 | yes | yes | SKIPPED (host-safety) |
| `trust: idempotent re-trust` | 0 | 0 | yes | yes | SKIPPED (host-safety) |
| `untrust: no entry found` | 0 | 0 | yes | yes | SKIPPED (host-safety) |
| `untrust: after trusting` | 0 | 0 | yes | yes | SKIPPED (host-safety) |
| `trust --list: after untrust (empty again)` | 0 | 0 | yes | yes | SKIPPED (host-safety) |
| `verify --require-all: built-ins only (all have tests, PASS)` | 0 | 0 | yes | yes | SKIPPED (host-safety) |
| `verify --filter: trusted project filter, passing test` | 0 | 0 | yes | yes | SKIPPED (host-safety) |
| `verify --filter: trusted project filter, FAILING test` | 0 | 0 | yes | yes | SKIPPED (host-safety) |
| `verify --filter: trusted project filter, no tests, no --require-all (skips, PASS)` | 0 | 0 | yes | yes | SKIPPED (host-safety) |
| `verify --filter --require-all: trusted project filter, no tests (MISSING bail)` | 0 | 0 | yes | yes | SKIPPED (host-safety) |

## Mismatch details

### `config: no file (defaults shown)`

- Verdict: **SKIPPED (host-safety)**
- Rust exit: `0`, .NET exit: `0`
- Rust stdout: *(empty)*
- .NET stdout: *(empty)*
- Skip reason: `%LOCALAPPDATA%\rtk already has real content on this host (this is also the oracle's usage-tracking-history directory, written on every invocation); refusing to risk touching it.`

### `config: pre-existing file (shows created content)`

- Verdict: **SKIPPED (host-safety)**
- Rust exit: `0`, .NET exit: `0`
- Rust stdout: *(empty)*
- .NET stdout: *(empty)*
- Skip reason: `%LOCALAPPDATA%\rtk already has real content on this host (this is also the oracle's usage-tracking-history directory, written on every invocation); refusing to risk touching it.`

### `config --create`

- Verdict: **SKIPPED (host-safety)**
- Rust exit: `0`, .NET exit: `0`
- Rust stdout: *(empty)*
- .NET stdout: *(empty)*
- Skip reason: `%LOCALAPPDATA%\rtk already has real content on this host (this is also the oracle's usage-tracking-history directory, written on every invocation); refusing to risk touching it.`

### `config --create (idempotent re-run)`

- Verdict: **SKIPPED (host-safety)**
- Rust exit: `0`, .NET exit: `0`
- Rust stdout: *(empty)*
- .NET stdout: *(empty)*
- Skip reason: `%LOCALAPPDATA%\rtk already has real content on this host (this is also the oracle's usage-tracking-history directory, written on every invocation); refusing to risk touching it.`

### `TOML fallback dispatch: built-in df filter`

- Verdict: **SKIPPED (host-safety)**
- Rust exit: `0`, .NET exit: `0`
- Rust stdout: *(empty)*
- .NET stdout: *(empty)*
- Skip reason: `%LOCALAPPDATA%\rtk already has real content on this host (this is also the oracle's usage-tracking-history directory, written on every invocation); refusing to risk touching it.`

### `trust: no .rtk/filters.toml present (fail-loud)`

- Verdict: **SKIPPED (host-safety)**
- Rust exit: `0`, .NET exit: `0`
- Rust stdout: *(empty)*
- .NET stdout: *(empty)*
- Skip reason: `%LOCALAPPDATA%\rtk already has real content on this host (this is also the oracle's usage-tracking-history directory, written on every invocation); refusing to risk touching it.`

### `trust --list: empty store`

- Verdict: **SKIPPED (host-safety)**
- Rust exit: `0`, .NET exit: `0`
- Rust stdout: *(empty)*
- .NET stdout: *(empty)*
- Skip reason: `%LOCALAPPDATA%\rtk already has real content on this host (this is also the oracle's usage-tracking-history directory, written on every invocation); refusing to risk touching it.`

### `trust: happy path (dump + risk summary + trusted)`

- Verdict: **SKIPPED (host-safety)**
- Rust exit: `0`, .NET exit: `0`
- Rust stdout: *(empty)*
- .NET stdout: *(empty)*
- Skip reason: `%LOCALAPPDATA%\rtk already has real content on this host (this is also the oracle's usage-tracking-history directory, written on every invocation); refusing to risk touching it.`

### `trust --list: after trusting`

- Verdict: **SKIPPED (host-safety)**
- Rust exit: `0`, .NET exit: `0`
- Rust stdout: *(empty)*
- .NET stdout: *(empty)*
- Skip reason: `%LOCALAPPDATA%\rtk already has real content on this host (this is also the oracle's usage-tracking-history directory, written on every invocation); refusing to risk touching it.`

### `trust: idempotent re-trust`

- Verdict: **SKIPPED (host-safety)**
- Rust exit: `0`, .NET exit: `0`
- Rust stdout: *(empty)*
- .NET stdout: *(empty)*
- Skip reason: `%LOCALAPPDATA%\rtk already has real content on this host (this is also the oracle's usage-tracking-history directory, written on every invocation); refusing to risk touching it.`

### `untrust: no entry found`

- Verdict: **SKIPPED (host-safety)**
- Rust exit: `0`, .NET exit: `0`
- Rust stdout: *(empty)*
- .NET stdout: *(empty)*
- Skip reason: `%LOCALAPPDATA%\rtk already has real content on this host (this is also the oracle's usage-tracking-history directory, written on every invocation); refusing to risk touching it.`

### `untrust: after trusting`

- Verdict: **SKIPPED (host-safety)**
- Rust exit: `0`, .NET exit: `0`
- Rust stdout: *(empty)*
- .NET stdout: *(empty)*
- Skip reason: `%LOCALAPPDATA%\rtk already has real content on this host (this is also the oracle's usage-tracking-history directory, written on every invocation); refusing to risk touching it.`

### `trust --list: after untrust (empty again)`

- Verdict: **SKIPPED (host-safety)**
- Rust exit: `0`, .NET exit: `0`
- Rust stdout: *(empty)*
- .NET stdout: *(empty)*
- Skip reason: `%LOCALAPPDATA%\rtk already has real content on this host (this is also the oracle's usage-tracking-history directory, written on every invocation); refusing to risk touching it.`

### `verify --require-all: built-ins only (all have tests, PASS)`

- Verdict: **SKIPPED (host-safety)**
- Rust exit: `0`, .NET exit: `0`
- Rust stdout: *(empty)*
- .NET stdout: *(empty)*
- Skip reason: `%LOCALAPPDATA%\rtk already has real content on this host (this is also the oracle's usage-tracking-history directory, written on every invocation); refusing to risk touching it.`

### `verify --filter: trusted project filter, passing test`

- Verdict: **SKIPPED (host-safety)**
- Rust exit: `0`, .NET exit: `0`
- Rust stdout: *(empty)*
- .NET stdout: *(empty)*
- Skip reason: `%LOCALAPPDATA%\rtk already has real content on this host (this is also the oracle's usage-tracking-history directory, written on every invocation); refusing to risk touching it.`

### `verify --filter: trusted project filter, FAILING test`

- Verdict: **SKIPPED (host-safety)**
- Rust exit: `0`, .NET exit: `0`
- Rust stdout: *(empty)*
- .NET stdout: *(empty)*
- Skip reason: `%LOCALAPPDATA%\rtk already has real content on this host (this is also the oracle's usage-tracking-history directory, written on every invocation); refusing to risk touching it.`

### `verify --filter: trusted project filter, no tests, no --require-all (skips, PASS)`

- Verdict: **SKIPPED (host-safety)**
- Rust exit: `0`, .NET exit: `0`
- Rust stdout: *(empty)*
- .NET stdout: *(empty)*
- Skip reason: `%LOCALAPPDATA%\rtk already has real content on this host (this is also the oracle's usage-tracking-history directory, written on every invocation); refusing to risk touching it.`

### `verify --filter --require-all: trusted project filter, no tests (MISSING bail)`

- Verdict: **SKIPPED (host-safety)**
- Rust exit: `0`, .NET exit: `0`
- Rust stdout: *(empty)*
- .NET stdout: *(empty)*
- Skip reason: `%LOCALAPPDATA%\rtk already has real content on this host (this is also the oracle's usage-tracking-history directory, written on every invocation); refusing to risk touching it.`

