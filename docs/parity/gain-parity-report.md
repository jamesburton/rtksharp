# `rtk gain` Oracle Parity Report

> Auto-generated by `RtkSharp.ParityTests/GainParityTests.cs` (Phase 5 Task 6 acceptance gate). Do not edit by hand — re-run the test to refresh.

- **Generated (UTC):** 2026-07-05 04:22:57
- **Platform:** Windows
- **Oracle:** `C:\Development\rtksharp\target\release\rtk.exe`
- **RtkSharp:** `C:\Development\rtksharp\RtkSharp.ParityTests\bin\Debug\net10.0\RtkSharp.exe`
- **Battery size:** 18 entries (default summary, daily/weekly/monthly/all, graph, history, 4 quota variants, json/csv format x2 each, failures x2, reset)

Comparison method: each entry seeds a fresh, byte-identical SQLite database (raw SQL insert against the shared `commands`/`parse_failures` schema, never through either binary) into its own per-side temp copy, then runs both binaries with `RTK_DB_PATH` pointed at that copy and `CLAUDE_CONFIG_DIR` redirected to a non-existent per-entry path (so `hook_check::status()` resolves deterministically to `Ok` on both sides), with `StdinContent = ""` (forced non-interactive stdin). Stdout (CRLF/LF normalized, trailing-whitespace trimmed) and exit code are compared; stderr is not (matches this project's established parity-harness convention — see `HookParityTests`/`RewriteParityTests`). No `HostRealDirGuard`/junction machinery is used: `RTK_DB_PATH` fully redirects the tracking database on the oracle with no fallback to the real `%LOCALAPPDATA%\rtk`, and `gain`'s own hook-status check (`hook_check::status()`) never writes to that directory — only `maybe_warn()` does, which `gain` never calls. This was verified empirically (a canary `rtk gain --format json` run with only `RTK_DB_PATH` set left every file under the real `%LOCALAPPDATA%\rtk` with an identical mtime before and after) before writing this battery — see the class remarks in `GainParityTests.cs` for detail.

## Summary

| Metric | Result |
|--------|--------|
| **Overall parity (headline)** | **100.0%** |
| Threshold | 95% |
| Entries matched | 18 / 18 |

## Per-entry results

| Entry | Rust exit | .NET exit | Stdout match | Verdict |
|-------|-----------|-----------|--------------|---------|
| `default summary (global scope)` | 0 | 0 | yes | MATCH |
| `--daily` | 0 | 0 | yes | MATCH |
| `--weekly` | 0 | 0 | yes | MATCH |
| `--monthly` | 0 | 0 | yes | MATCH |
| `--all (daily+weekly+monthly tables)` | 0 | 0 | yes | MATCH |
| `--graph (default view + Daily Savings graph)` | 0 | 0 | yes | MATCH |
| `--history (default view + Recent Commands)` | 0 | 0 | yes | MATCH |
| `--quota (default tier 20x)` | 0 | 0 | yes | MATCH |
| `--quota -t 5x` | 0 | 0 | yes | MATCH |
| `--quota -t pro` | 0 | 0 | yes | MATCH |
| `--quota -t bogus (invalid tier, silent pro fallback)` | 0 | 0 | yes | MATCH |
| `--format json (summary only)` | 0 | 0 | yes | MATCH |
| `--format json --all` | 0 | 0 | yes | MATCH |
| `--format csv (no period flags -> empty stdout)` | 0 | 0 | yes | MATCH |
| `--format csv --all` | 0 | 0 | yes | MATCH |
| `--failures (zero failures)` | 0 | 0 | yes | MATCH |
| `--failures (with failures)` | 0 | 0 | yes | MATCH |
| `--reset --yes` | 0 | 0 | yes | MATCH |

## Mismatch details

None — every battery entry matched byte-exact stdout and an identical exit code.
