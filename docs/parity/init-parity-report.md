# `rtk init` Installer Oracle Parity Report

> Auto-generated by `RtkSharp.ParityTests/InitParityTests.cs` (Phase 9b Task 4 acceptance gate). Do not edit by hand — re-run the test to refresh.

- **Generated (UTC):** 2026-07-04 02:54:03
- **Platform:** Windows
- **Oracle:** `C:\Development\rtksharp\target\release\rtk.exe`
- **RtkSharp:** `C:\Development\rtksharp\RtkSharp.ParityTests\bin\Release\net10.0\RtkSharp.exe`
- **Battery size:** 13 entries (6 project-scope + 7 global-scope)

Comparison method: each entry runs both binaries in a fresh, isolated temp directory (`CLAUDE_CONFIG_DIR` redirected for both; global-scope entries also redirect the global filters-template path via `RTK_CONFIG_DIR_OVERRIDE` for the port and `XDG_CONFIG_HOME` for the oracle on Linux/macOS) with `StdinContent = ""` (forced non-interactive stdin). Multi-step entries (install-then-uninstall, repeated-run idempotency) compare only the final step's stdout (CRLF/LF normalized) and exit code. Write entries additionally compare a full relative-path -> SHA-256 manifest of the temp directory after all steps.

> **Windows global-filters-template asymmetry.** The Rust oracle has no config-dir override; `dirs::config_dir()` resolves via a Win32 known-folder API that ignores environment variables, so on Windows the oracle's `generate_global_filters_template` writes to the real `%APPDATA%\rtk\filters.toml` (outside the temp sandbox) while the port writes to the redirected `.config-rtk/rtk/filters.toml` inside the temp sandbox. This harness backs up/restores the real file around every affected oracle run (see `WindowsGlobalFiltersGuard`) and excludes that one relative path from the Windows tree-diff — every other file in the tree is still compared byte-exact. See `docs/parity/compatibility-ledger.md` for the ledgered entry.

## Summary

| Metric | Result |
|--------|--------|
| **Overall parity (headline)** | **100.0%** |
| Threshold | 95% |
| Entries matched | 13 / 13 |

## Per-entry results

| Entry | Rust exit | .NET exit | Stdout match | Tree match | Verdict |
|-------|-----------|-----------|--------------|------------|---------|
| `project: default init` | 0 | 0 | yes | yes | MATCH |
| `project: init --claude-md` | 0 | 0 | yes | yes | MATCH |
| `project: init --hook-only (warn path)` | 0 | 0 | yes | yes | MATCH |
| `project: init --dry-run -v` | 0 | 0 | yes | yes | MATCH |
| `project: init --uninstall (after default install)` | 1 | 1 | yes | yes | MATCH |
| `project: re-run idempotency` | 0 | 0 | yes | yes | MATCH |
| `global: default init -g` | 0 | 0 | yes | yes | MATCH |
| `global: init -g --auto-patch` | 0 | 0 | yes | yes | MATCH |
| `global: init -g --no-patch` | 0 | 0 | yes | yes | MATCH |
| `global: init -g --dry-run` | 0 | 0 | yes | yes | MATCH |
| `global: init -g --uninstall (after default install)` | 0 | 0 | yes | yes | MATCH |
| `global: init -g --show` | 0 | 0 | yes | yes | MATCH |
| `global: re-run idempotency` | 0 | 0 | yes | yes | MATCH |

## Mismatch details

None — every battery entry matched byte-exact stdout, an identical exit code, and (for write entries) an identical produced file tree.
