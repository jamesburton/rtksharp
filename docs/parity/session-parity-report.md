# `rtk session` Oracle Parity Report

> Auto-generated by `RtkSharp.ParityTests/SessionParityTests.cs`. Do not edit by hand — re-run the test to refresh.

- **Generated (UTC):** 2026-07-06 01:24:32
- **Platform:** Windows
- **Oracle:** `C:\Development\rtksharp\target\release\rtk.exe`
- **RtkSharp:** `C:\Development\rtksharp\RtkSharp.ParityTests\bin\Debug\net10.0\RtkSharp.exe`
- **Battery size:** 6 entries (no sessions, mixed explicit/hook/unsupported, non-Bash-only session, chained command split, multi-project)

Comparison method: each entry writes identical synthetic `.jsonl` session-transcript fixtures into a per-side `CLAUDE_CONFIG_DIR/projects/<dir>/<file>.jsonl` tree (a real, Rust-honored override — no junction/guard machinery needed), then runs both binaries with `CLAUDE_CONFIG_DIR` pointed at that tree. Stdout (CRLF/LF normalized, trailing-whitespace trimmed) and exit code are compared; stderr is not.

## Summary

| Metric | Result |
|--------|--------|
| **Overall parity (headline)** | **100.0%** |
| Threshold | 100% |
| Entries matched | 6 / 6 |

## Per-entry results

| Entry | Rust exit | .NET exit | Stdout match | Verdict |
|-------|-----------|-----------|--------------|---------|
| `no projects directory at all` | 0 | 0 | yes | MATCH |
| `one session: mixed explicit-rtk, hook-rewritable, and unsupported commands` | 0 | 0 | yes | MATCH |
| `session with only non-Bash tool activity (Read/Grep) -> zero commands found` | 0 | 0 | yes | MATCH |
| `chained command (&&) split across two logical commands, one supported one not` | 0 | 0 | yes | MATCH |
| `two sessions across two different project directories` | 0 | 0 | yes | MATCH |
| `adoption percentage lands exactly on a .5 rounding tie (8 total, 1 rtk -> 12.5%)` | 0 | 0 | yes | MATCH |

## Mismatch details

None — every battery entry matched byte-exact stdout and an identical exit code.
