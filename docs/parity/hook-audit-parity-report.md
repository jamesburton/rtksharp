# `rtk hook-audit` Oracle Parity Report

> Auto-generated by `RtkSharp.ParityTests/HookAuditParityTests.cs`. Do not edit by hand — re-run the test to refresh.

- **Generated (UTC):** 2026-07-05 18:43:18
- **Platform:** Windows
- **Oracle:** `C:\Development\rtksharp\target\release\rtk.exe`
- **RtkSharp:** `C:\Development\rtksharp\RtkSharp.ParityTests\bin\Debug\net10.0\RtkSharp.exe`
- **Battery size:** 9 entries (no log, empty log, malformed-only, mixed valid+malformed with distinct skip/top-command counts, empty-window --since, now-relative --since window, env-var-prefixed base-command grouping)

Comparison method: each entry writes an identical (or absent) `hook-audit.log` into its own per-side temp directory, then runs both binaries with `RTK_AUDIT_DIR` pointed at that directory and `CLAUDE_CONFIG_DIR` redirected to a non-existent per-entry path (so `hook_check::status()` resolves deterministically to `Ok` on both sides, matching the established `PipeProxyRunParityTests` convention). Stdout (CRLF/LF normalized, trailing-whitespace trimmed, temp audit-dir path substituted with a placeholder) and exit code are compared; stderr is not (matches this project's established parity-harness convention).

## Summary

| Metric | Result |
|--------|--------|
| **Overall parity (headline)** | **100.0%** |
| Threshold | 100% |
| Entries matched | 9 / 9 |

## Per-entry results

| Entry | Rust exit | .NET exit | Stdout match | Verdict |
|-------|-----------|-----------|--------------|---------|
| `no audit log at all` | 0 | 0 | yes | MATCH |
| `empty log file` | 0 | 0 | yes | MATCH |
| `only malformed lines` | 0 | 0 | yes | MATCH |
| `mixed valid+malformed, distinct skip/top-command counts, all time` | 0 | 0 | yes | MATCH |
| `--since default (7, via -s short flag), all entries far in the past -> empty window` | 0 | 0 | yes | MATCH |
| `--since=3 (equals form), 'now'-relative timestamps inside and outside the window` | 0 | 0 | yes | MATCH |
| `env-var-prefixed original commands feed base_command grouping` | 0 | 0 | yes | MATCH |
| `skip-reason padding clamp at/over the 13-char threshold` | 0 | 0 | yes | MATCH |
| `-v (verbose) before the verb appends the trailing Log: path line` | 0 | 0 | yes | MATCH |

## Mismatch details

None — every battery entry matched byte-exact stdout and an identical exit code.
