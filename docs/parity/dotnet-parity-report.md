# dotnet-Command Oracle Parity Report

> Auto-generated by `RtkSharp.ParityTests/DotnetParityTests.cs` (Phase 6a acceptance gate). Do not edit by hand — re-run the test to refresh.

- **Generated (UTC):** 2026-07-03 02:57:28
- **Oracle:** `C:\Development\rtksharp\target\release\rtk.exe`
- **RtkSharp:** `C:\Development\rtksharp\RtkSharp.ParityTests\bin\Debug\net10.0\RtkSharp.exe`
- **Working directory (both sides):** repo root
- **Battery size:** 7 commands

Comparison method: stdout with CRLF/LF normalized and tee-hint lines stripped; duration tokens (`(HH:MM:SS.ff)`, `(N.N s)`, `(N ms)`, `(unknown)`, the bare `Time Elapsed HH:MM:SS.ff` line on failed builds), rtk's random TRX results file, and (for the two failure entries) the throwaway temp-project root — replaced with `<TMP>` — are masked on both sides; line-parity % = matching lines / max(oracle, dotnet). Stderr is ignored (oracle prints a hook warning there). Build-state determinism: a plain `dotnet build RtkSharp.slnx` warms the tree (and each temp project is warmed) before capture so both sides see the incremental no-op shape; oracle and port run back-to-back per entry.

Failure entries: `dotnet build <TMP>\broken\Broken.csproj` (deliberate CS1525) and `dotnet test <TMP>\failing\FailingTests.csproj` (a failing xunit `Assert.True(false)`) are scaffolded in a per-run temp tree OUTSIDE the repo and deleted afterwards. Both sides exit non-zero with matching failure summaries.

Scoped allowance 1 (project-count, binlog deferral): the two multi-project `*.slnx` entries mask the `N projects` summary token on both sides. The Rust oracle reports the full build-graph node count from the MSBuild binary log (`4 projects` — the binlog enumerates 4 build-graph nodes, whereas the `*.slnx` itself lists 3 projects); RtkSharp defers binlog and counts `.csproj` text mentions (`1`/`0 projects` on a no-op build) — the exact class ledgered in `docs/parity/compatibility-ledger.md`.

Scoped allowance 2 (diagnostic-detail, binlog garble): the broken-build entry masks the indented detail lines of rtk's compact `Errors:` section on both sides. The oracle sources them from the binlog and garbles the location to a raw byte offset with no filename (`CS1525(50,5)`); RtkSharp shows the REAL `Program.cs(5,17)` via the drive-letter text fix — strictly better detail, also ledgered. Error/warning counts, the raw `Build FAILED` diagnostics, the verdict and the exit code are compared strictly; single-project passing entries and the failing-test entry get NO allowance.

## Summary

| Metric | Result |
|--------|--------|
| **Overall line parity (headline)** | **100.0%** |
| Threshold | 90% |
| Lines matched | 43 / 43 |
| Commands at 100% | 7 / 7 |

## Per-command results

| Command | Rust exit | .NET exit | Line parity % | Allowance | Verdict |
|---------|-----------|-----------|---------------|-----------|---------|
| `dotnet build RtkSharp/RtkSharp.csproj` | 0 | 0 | 100.0% | — | MATCH |
| `dotnet build RtkSharp.slnx` | 0 | 0 | 100.0% | project-count (binlog) | MATCH (project-count masked, binlog deferral) |
| `dotnet restore RtkSharp.slnx` | 0 | 0 | 100.0% | project-count (binlog) | MATCH (project-count masked, binlog deferral) |
| `dotnet test RtkSharp.Tests` | 0 | 0 | 100.0% | — | MATCH |
| `dotnet format RtkSharp.slnx --verify-no-changes` | 2 | 2 | 100.0% | — | MATCH |
| `dotnet build <TMP>\broken\Broken.csproj` | 1 | 1 | 100.0% | diagnostic-detail (binlog garble) | MATCH (compact diagnostic detail masked, binlog garble) |
| `dotnet test <TMP>\failing\FailingTests.csproj` | 1 | 1 | 100.0% | — | MATCH |

## Mismatch details

None — every battery command matched line-for-line (after normalization, tee-hint stripping, duration/temp masking, and the scoped project-count allowance on the two `*.slnx` entries) with identical exit codes.
