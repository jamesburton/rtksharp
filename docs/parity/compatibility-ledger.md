# RtkSharp / RTK (Rust) Compatibility Ledger

Phase 0 living document. Tracks (1) which of the seven Global-Constraints
parity categories have golden fixtures and where those fixtures live, and
(2) known/accepted output differences between the Rust `rtk` binary and the
in-progress `RtkSharp` .NET port. Later phases append to both sections as
filtering behavior is actually implemented — this document does not attempt
to be exhaustive at Phase 0, since most of RtkSharp's filter modules don't
exist yet.

## Step 1 survey: existing `tests/fixtures/` coverage

`tests/fixtures/` (31 files, pre-existing before this task) was surveyed
first, per the task brief, to avoid recapturing coverage that already exists.

| Required category | Already covered by `tests/fixtures/`? | Notes |
|---|---|---|
| rewrite cases | No | No rewrite-input fixtures present; existing files are all captured *command output* (git/gh/maven/gradle/etc.), not rewrite-engine input strings. |
| compound shell commands | No | Not represented; nearest existing coverage is `RtkSharp.Tests/Rewrite/ShellLexerTests.cs`'s in-code test strings (not under `tests/fixtures/`). |
| pipe safety | No | Same as above — no dedicated pipe-input fixtures existed. |
| `gh` commands | No | `tests/fixtures/` has `glab_*` (GitLab CLI) fixtures but no `gh` (GitHub CLI) fixtures. |
| `dotnet build/test/restore/format` | Partially | `tests/fixtures/dotnet/` (5 files) exists but only covers **failure/edge** paths: `build_failed.txt`, `test_failed.txt`, `format_changes.json`, `format_empty.json`, `format_success.json`. There was no `dotnet build` **success**, `dotnet test` **success**, or `dotnet restore` output captured. Gap filled in Step 2 below with real captures from this repo. |
| `ls/read/find/grep` | No | Not represented in `tests/fixtures/`. |
| TOML filter application | N/A (by design) | Not applicable as a *captured-output* fixture category — see the dedicated note in Fixture Coverage below; the 63 `src/filters/*.toml` files already embed `[[tests.<name>]]` blocks with `input`/`expected` pairs and are the intended source of truth per the task brief (avoids duplicating fixture data into a second location). |

Conclusion: only `dotnet build/test/restore/format` had partial existing
coverage (failure paths only); all other categories needed new fixtures,
captured in `tests/parity/fixtures/` (Step 2).

## Fixture Coverage

| Category | Source of truth | What's captured |
|---|---|---|
| rewrite cases | new `tests/parity/fixtures/rewrite-cases/example-inputs.txt` | 13 real candidate command strings, one per representative `pattern` regex in `src/discover/rules.rs` (git, gh, glab, cargo, pnpm, npm, cat, grep, ls, find, go), with a header cross-referencing the exact `rules.rs` line numbers. These are rewrite-engine *inputs*, not command output. |
| compound shell commands | new `tests/parity/fixtures/compound-shell/example-inputs.txt` | 9 real compound-command strings (`&&`, `\|\|`, `;` combinations, including `if`/`then`/`fi` and multi-stage cargo chains) copied verbatim from existing `RtkSharp.Tests/Rewrite/ShellLexerTests.cs` test-input literals, per the task brief's explicit instruction to reuse rather than invent. Header cites exact source line numbers. |
| pipe safety | new `tests/parity/fixtures/pipe-safety/example-inputs.txt` | 4 real piped-command strings (`\|`) copied verbatim from `RtkSharp.Tests/Rewrite/ShellLexerTests.cs` test-input literals. Header cites exact source line numbers. |
| `gh` commands | new `tests/parity/fixtures/gh-commands/gh_pr_list_raw.txt`, `gh_repo_view_raw.json` | Real captured output of `gh pr list --limit 5` and `gh repo view --json name,owner,description,url,defaultBranchRef`, run against this repo (`rtk-ai/rtk`) with an authenticated `gh` CLI (`gh version 2.75.1`, account `jamesburton`). Exit code 0 for both. |
| `dotnet build/test/restore/format` | existing `tests/fixtures/dotnet/` (failure paths, pre-existing) + new `tests/parity/fixtures/dotnet-workflow/` (success paths, this task) | New files: `dotnet_build_success_raw.txt` (`dotnet build RtkSharp.slnx`, exit 0, "Build succeeded"), `dotnet_test_success_raw.txt` (`dotnet test RtkSharp.Tests`, exit 0, 146/146 passed), `dotnet_restore_raw.txt` (`dotnet restore RtkSharp.slnx`, exit 0). Format success/failure and build/test failure paths remain covered by the pre-existing `tests/fixtures/dotnet/` files — not recaptured. |
| `ls/read/find/grep` | new `tests/parity/fixtures/ls-read-find-grep/` | Real captured output of `ls -la` (repo root), `grep -n "fn main" src/main.rs`, `find src/cmds -maxdepth 1 -type d`, and `cat RtkSharp.slnx` (stand-in for `read`), all run in this repo. |
| TOML filter application | `src/filters/*.toml` embedded `[[tests.*]]` blocks (not duplicated here) | Confirmed all 63 TOML filter files under `src/filters/` contain `[[tests.` blocks (`grep -l '\[\[tests\.' src/filters/*.toml` returns 63/63). Per the task brief, this category is intentionally *not* re-captured into `tests/parity/fixtures/` — Task 6's parity runner should read `input`/`expected` pairs directly from `src/filters/*.toml` to avoid a second, driftable copy of ~63 files' worth of test data. |

## Known Acceptable Differences

Populated with differences already confirmed during Phase 1/2 work. Expected
to stay short until RtkSharp implements actual filtering (Phase 4+) — not
padded with speculative entries.

| Area | Rust behavior | RtkSharp behavior | Why acceptable |
|---|---|---|---|
| Merged stdout/stderr line endings | Expected byte-exact passthrough of whatever the child process wrote (Rust's `std::process::Command::output()` returns separate, unmodified stdout/stderr byte buffers with no line-ending translation on read; the exact merge path — if any — was not located in `src/core/utils.rs`/`tee.rs` in this pass, so this is the brief's characterization rather than code-verified). | `RtkSharp/Execution/ProcessExecutor.cs`'s `AppendMergedLine` (confirmed at the current `port/dotnet-phase0` HEAD) builds the merged buffer via `StringBuilder.AppendLine(line)` on each `OutputDataReceived`/`ErrorDataReceived` callback, which appends `Environment.NewLine` (`\r\n` on Windows) regardless of the child's original line-ending bytes. | Line-ending normalization in the merged-capture path is a deliberate, low-risk simplification of .NET's event-based `OutputDataReceived` API (which already strips the original terminator per line); it does not affect any filter logic that operates on already-split lines, and is expected to be revisited only if a future filter needs byte-exact merged output. |
| Fixture file line endings (git normalization) | N/A — this is a fixture-storage concern, not a Rust-vs-RtkSharp behavior difference. | N/A | `tests/parity/fixtures/**/*.txt` and `.json` files were captured on Windows (CRLF) and may be subject to this repo's `.gitattributes` line-ending normalization on commit. **RESOLVED (Task 5):** `RewriteParityTests.cs` normalizes CRLF/LF (`Normalize()`) before byte-comparing stdout. The placeholder self-comparison note below (original `ParityRunner.CompareAsync`) is superseded for the rewrite gate, which drives the real RtkSharp binary. |
| ~~`rtk rewrite` permission verdict / exit code (host allow-list)~~ **— CLOSED (Phase 9a, Task 1)** | Oracle loads the developer's `~/.claude/settings.json`. Its `Bash(git:*)` allow-rule makes `check_command` return `Allow`, so `rtk rewrite "git status"` / `"git log"` exit **0** (auto-allow). | ~~RtkSharp implements no config system, so every rewritable command yields `Ask` → exit **3**.~~ **RESOLVED:** RtkSharp now loads the same `~/.claude/settings.json` (and project/local + `settings.local.json`) allow/deny/ask Bash rules via `RtkSharp/Rewrite/PermissionRules.cs` (port of `load_permission_rules`), so `git status`/`git log` now Allow → exit **0** on **both** sides. Stdout was always byte-identical. | **No longer a divergence.** Phase 9a Task 1 ported `load_permission_rules` (settings-file discovery, `Bash(...)` extraction, deny/ask/allow merge) and wired `Permissions.CheckCommand` to it. Rewrite-parity strict stdout+exit is now **100%** (`docs/parity/rewrite-parity-report.md`); the former 2-of-26 exit-code delta is gone. `RewriteParityTests.cs`'s `IsHostAllowDeviation` allowance is retained only as a defensive fallback and no longer fires. Row kept for history — do not re-open unless permission-rule loading is removed. (`~/.config/rtk/config.toml` `exclude_commands`/`transparent_prefixes` remain deferred, but do not affect these fixture lines.) |
| `rtk find` gitignore handling | `ignore` crate with `git_global(true)` (honors `core.excludesFile`) | Hierarchical `.gitignore` + `.git/info/exclude` only; no global excludesFile | Narrow, host-config-dependent; port targets repo-level ignore semantics; revisit if parity battery hits it. |
| `rtk grep -rln` / files-with-matches line order | Emits the file list in ripgrep's parallel directory-walker order, which is **run-to-run nondeterministic** (verified in Task 6: the *same* `target/release/rtk.exe` returned `RewriteEngine.cs, ShellLexer.cs` on 3 of 5 runs and the reverse on the other 2). | Identical: shells out to the same `rg` binary with the same flags (files-with-matches modes go through `GrepCommand`'s native passthrough), so it inherits the same nondeterministic ordering. | Line **order** in a files-with-matches listing is not part of the contract — the deterministic invariant is the *set* of matching files, which is byte-identical on both sides. `SystemParityTests.cs` treats an output pair that is equal as a sorted line multiset but unequal in index order as an ordering-only deviation (counted as matched, labelled `MATCH (rg ordering, nondeterministic)`); a missing/extra/changed line changes the multiset and still fails. Not a port bug: both sides delegate ordering to `rg`. |
| `rtk ls` / `rtk wc` exit code when coreutils are absent | Native `ls`/`wc` are spawned via the Rust `std::process::Command`; a spawn failure (binary not on PATH) surfaces as process exit **1**. | `LsCommand`/`WcCommand` shell out through `ProcessExecutor`, which maps a failed `Process.Start` (Win32Exception/"did not start") to exit **127** (the conventional "command not found" code). Filtered stdout is empty on both sides in this case. | Environment-only: manifests solely when the Unix coreutils are missing from PATH, which contradicts rtk's deployment premise (it exists to compress `ls`/`wc`/`grep` output, so those tools must be installed). `SystemParityTests.cs` prepends the Git-for-Windows `usr\bin` directory to the PATH of **both** child binaries so the filters run against real output and both exit 0; the delta is therefore not exercised. The exit-code mapping (1 vs 127) is a defensible .NET convention difference, documented here rather than "fixed" to a less-standard value. Revisit only if byte-exact spawn-failure exit parity is later required. |
| `rtk dotnet build` diagnostics on Windows-absolute paths | The text parser's `ISSUE_RE` (`src/cmds/dotnet/binlog.rs:56`) captures the file group as `[^:(]+`, which cannot cross a drive-letter colon (`C:\...`), so a Windows-absolute-path diagnostic line fails to match cleanly and the compact `Errors:`/`Warnings:` sections fall back to a `Build error #N (details omitted)` / `Build warning #N (details omitted)` placeholder. In practice this text-parser bug is masked because the Rust binary's default path injects `-bl:<tmp>` and parses the MSBuild binary log instead, which carries structured file paths and never hits this text-regex limitation. | `IssueRegex` in `DotnetCommand.cs` widens the file-capturing group to `(?:[A-Za-z]:)?[^\r\n:(]+`, an optional drive-letter prefix that lets the group cross the colon. Windows-absolute-path diagnostics now produce real `file`/`line`/`column`/`code`/`message` details instead of the placeholder; Unix/relative paths are unaffected since the prefix is optional. | Intentional, ledgered improvement (not a silent behavior drift): strictly more informative than the Rust oracle on Windows, and byte-identical to it on Unix paths. Confirmed via reviewer sign-off; covered by `DotnetCommandTests.FilterBuild_WindowsDriveLetterPath_ExtractsRealDetailsNotPlaceholder` and `ParseBuildFromText_WindowsDriveLetterPath_ExtractsRealDetails`. The underlying reason the Rust oracle avoids this bug (binlog parsing) remains deferred in RtkSharp — see the next row. |
| `rtk dotnet build` binlog `extract_binary_like_issues` salvage branch | `src/cmds/dotnet/binlog.rs:1064`'s `extract_binary_like_issues` is a salvage branch inside the deferred binary-log reader: it only fires when the parsed issue list is empty **and** the build failed **and** a binary-level error signal is present in the log, scanning the raw binlog bytes for embedded diagnostic-like strings as a last resort. | Not ported — RtkSharp's build/restore filters use the text-only path (`-bl` binlog injection itself is deferred; see the class remarks in `DotnetCommand.cs`), so there is no binary log to salvage from. | Deliberate deferral, not a gap: the salvage branch is a rare last-resort fallback for a code path (binlog reading) that is already out of scope this phase, and its trigger conditions are narrow (empty issues + failed build + binary error signal). RtkSharp's `N Error(s)`/`N Warning(s)` inline-count fallback (`ErrorCountRegex`/`WarningCountRegex` in `ParseBuildFromText`) already covers the count-visibility need for failed builds with no per-line diagnostics, so real-world impact is near-zero. Revisit only if/when binlog parsing itself is ported. |
| `rtk dotnet build`/`restore` **project count** on a multi-project, up-to-date (no-op) `*.slnx` | The oracle's default path parses the MSBuild **binary log**, which enumerates every build-graph node in the solution, so the summary reports the full count regardless of incrementality — e.g. `ok dotnet build: 4 projects, …` and `ok dotnet restore: 4 projects, …` for `RtkSharp.slnx` even when nothing rebuilt. (The binlog enumerates **4 build-graph nodes**; the `*.slnx` itself lists only **3 projects** — `RtkSharp`, `RtkSharp.Tests`, `RtkSharp.ParityTests` — the 4th node is the extra build-graph vertex MSBuild adds for the solution/restore graph. The token is `4 projects` regardless.) | RtkSharp defers binlog and counts project mentions in the console **text** via `ProjectPathRegex` (byte-identical to Rust's `PROJECT_PATH_RE` text fallback). On a no-op multi-project build the console text names only the primary/driver project, so the count under-reports: `1 projects` for `build`, `0 projects` for `restore` (restore emits no per-project text line when all are up to date). Single-project targets (`build RtkSharp.csproj`, `test RtkSharp.Tests`) are unaffected — both sides report `1 projects`. | Direct, expected consequence of the deferred binlog (see the two rows above): the *only* divergent token is the leading `N projects` integer on the summary line; error/warning counts, diagnostics, verdict icon, and exit code are byte-identical. Not a port bug — the text project-counting regex is a faithful port of Rust's own text fallback; the oracle simply has a richer source (binlog) that RtkSharp does not read this phase. `DotnetParityTests.cs` tolerates this **only** for the two multi-project `*.slnx` battery entries via a per-entry `AllowProjectCountDivergence` flag that masks the `N projects` token on both sides (`ProjectCountRegex` → `(PROJECTS)`); single-project entries do not get the allowance and match the count exactly. Not globally masked. Revisit if/when binlog parsing is ported (RtkSharp would then match the full count). |
| `rtk git` failure-path stderr **trailing blank line** (`eprintln!` double-newline) | On a failed `git add`/`pull`/`fetch`/`branch`/`stash`/`worktree`, Rust prints captured child stderr with `eprintln!("{}", result.stderr)` (git.rs `run_add` line 970 and siblings). Because `result.stderr` already ends in `\n`, `eprintln!` appends a second newline, leaving a **spurious trailing blank line** — observed on `git add nonexistent-file.txt`: stderr is `FAILED: git add\nfatal: pathspec '…' did not match any files\n\n`. | The port (`GitCommand.cs` `RunAddAsync` etc.) writes `result.Stderr.TrimEnd('\n') + "\n"`, trimming the child's trailing newline before re-adding exactly one — so no spurious blank line: `FAILED: git add\nfatal: pathspec '…' did not match any files\n`. | Intentional, documented normalization (Task 4 acceptance-gate decision — the plan explicitly permitted "fix the port to match Rust exactly OR ledger it as an intentional normalization"). The port's output is strictly cleaner and the divergence is trailing-whitespace only. It never surfaces in comparison: the Group-C entries in `GitParityTests.cs` are compared after the harness's standard `TrimEnd('\n')` (the same trailing-newline trim the `SystemParityTests`/`DotnetParityTests` harnesses apply), which absorbs the trailing blank line identically on both sides. Both `git add nonexistent-file.txt` and `git push nonexistent-remote master` reach 100% stdout+stderr parity, exit 128 on both sides. Not fixed in the port because the current behavior is the more correct one; revisit only if byte-exact stderr replication (including Rust's spurious blank line) is ever required. Note: this normalization was deliberately NOT extended to `log`/`diff`/`show`/`commit`/`status`, whose failure paths replicate their exact Rust stderr idioms (including any spurious blank line) — fidelity retained there by design, not oversight. |
| `rtk dotnet build` compact **`Errors:` detail line** on a FAILED build (binlog garble vs real text) | The oracle sources the compact `Errors:` section from the MSBuild **binary log** and, on a Windows-absolute-path diagnostic, garbles the location to a raw byte offset with **no filename** — observed `  CS1525(50,5) error: Invalid expression term ';'` for a `dotnet build` of a project whose real error is `Program.cs(5,17)`. (The `(50,5)` is a binlog byte offset, not a source line/column.) | RtkSharp parses the console **text** with the drive-letter-aware `IssueRegex` (see the drive-letter row above) and emits the **real** file/line/column in the compact section — `  <file>Program.cs(5,17) error CS1525: Invalid expression term ';' [<proj>]` — i.e. strictly **more informative** than the oracle. | Intentional, ledgered *improvement* (same root cause as the drive-letter row): RtkSharp is strictly better than the oracle's garbled binlog rendering on Windows-absolute paths. Error/warning **counts**, the raw `Build FAILED` diagnostic lines (byte-identical on both sides), the `fail dotnet build: … 1 errors …` verdict, and the exit code all match strictly. `DotnetParityTests.cs` tolerates only the compact `Errors:`/`Warnings:` **detail** lines on the single broken-build failure entry via a per-entry `AllowDiagnosticDetailDivergence` flag that masks those indented lines on both sides while **preserving their count** (so diagnostic-count parity is still enforced). The failing-test entry needs no such allowance — the test runs on both sides and the failure summary is byte-identical once the random TRX results path is masked. Revisit if/when binlog parsing is ported. |
| `rtk init -g` **global filters-template write target on Windows** (`generate_global_filters_template`) | `dirs::config_dir()` resolves via `SHGetKnownFolderPath(FOLDERID_RoamingAppData)`, a Win32 known-folder API that ignores environment variables entirely — there is no config-dir override on the Rust side. So on Windows the oracle's global-scope `init -g` (default/`--auto-patch`/`--no-patch` modes) would otherwise always write the filters template to the **real** `%APPDATA%\rtk\filters.toml`, outside any test sandbox. | RtkSharp's `InitArtifacts.ResolveGlobalConfigDir` checks the test-only `RTK_CONFIG_DIR_OVERRIDE` env var (built in Task 1) before falling back to the real OS config dir, so under the parity harness it writes to the redirected `{temp}/.config-rtk/rtk/filters.toml` instead. | Structural asymmetry, not a port bug: only the *port* is redirectable on Windows (by design — the override is test/parity-only plumbing, per Resolved Questions §1 of `docs/superpowers/plans/2026-07-03-phase9b-init-hooks.md`). **Task 4 safety fix (superseding the original snapshot/restore approach):** a review found the original `WindowsGlobalFiltersGuard` (backup the real file, run the oracle unprotected, restore in `Dispose`) unsafe — the restore only ran on a normal `using`-block exit, so a crashed test host or killed CI job could leave the developer's real file mutated with no restoration, and a freshly-created `%APPDATA%\rtk` directory was never cleaned up even on the happy path. The guard now redirects `%APPDATA%\rtk` into the oracle's own temp sandbox via an NTFS directory junction (`mklink /J`) *before* the oracle ever runs, so its write physically lands inside the sandbox and the real host profile is never touched at all — there is nothing to back up or restore. Tearing down the junction (`rmdir`, no `/s`) only ever removes the reparse point, never the (sandboxed) target's content, so even a failed cleanup cannot lose real data; a marker file plus a one-time static self-heal check detects and removes any junction left dangling by a prior crash, and `AppDomain.ProcessExit`/`UnhandledException` handlers add a second cleanup layer. Because the write now lands inside the oracle's own temp directory, the produced tree is byte-exact for **every** file including the filters template — the former `.config-rtk/rtk/filters.toml` tree-diff exclusion is no longer needed in the common case. Two narrow fallbacks remain, both zero-touch: if `%APPDATA%\rtk\filters.toml` already exists on the host, the guard is a no-op (the oracle's own "already exists, skip" branch means it never writes, so nothing needs redirecting) and only that one path/stdout line is excluded from comparison; if `%APPDATA%\rtk` exists as a real directory without `filters.toml`, the entire entry is skipped (reported as `SKIPPED (host-safety)`) rather than ever risking a real write. Neither fallback was exercised on the verification machine (a clean `%APPDATA%\rtk` took the fully-redirected path, 13/13 entries matched at 100%). Non-Windows platforms have no asymmetry: `XDG_CONFIG_HOME` redirects the oracle's `dirs::config_dir()` too, landing at the identical relative path as the port's override. |
| ~~`rtk verify` (no `--filter`) missing TOML inline-test battery~~ **— CLOSED (Phase 4, Task 5)** | `Commands::Verify`'s match arm (`main.rs`:2523-2536) unconditionally runs **both** `hooks::integrity::run_verify(cli.verbose)` **and** `hooks::verify_cmd::run(None, require_all)` whenever `filter.is_none()` — the latter runs every TOML filter's inline `[[tests.*]]` battery (`src/hooks/verify_cmd.rs`) and prints a trailing `"{passed}/{total} tests passed"` (or `"No inline tests found."`) block, failing loudly on any inline-test failure or (with `--require-all`) any filter lacking tests. | ~~`RtkSharp/Hooks/VerifyCommand.cs` only calls `Integrity.RunVerify(verbose)`; `hooks::verify_cmd::run` is not ported; `--filter`/`--require-all` rejected as not-yet-implemented.~~ **RESOLVED:** `VerifyCommand.cs` now parses `--filter <name>`/`--require-all` and dispatches the real battery via `TomlFilterEngine.RunFilterTests` (faithful port of `toml_filter::run_filter_tests` — built-in blob + trust-gated project `.rtk/filters.toml`, alphabetical BTreeMap order, `trim_end_matches('\n')` compare). `RunInlineTestsCore` reproduces `verify_cmd.rs:11-49` byte-for-byte: per-failure `FAIL [{filter}] {name}\n  expected: {:?}\n  actual:   {:?}` to stderr, `"{passed}/{total} tests passed"` / `"No inline tests found."` to stdout, per-missing `MISSING tests for filter: {name}` + bail `"{n} filter(s) have no inline tests (use --require-all in CI)"` (with `--require-all`), and failed-test bail `"{n} test(s) failed"` — both rendered as `rtk: {msg}` (exit 1) by `Run`. Bare `rtk verify` runs integrity first, then the battery. | **No longer a gap.** Closed in `feat(hooks): wire config and TOML filters into rewrite/grep/dispatch, close rtk verify inline-test ledger gap`. The only intentional narrowing vs the oracle: tee/raw-recovery hinting is not emitted (the tee store remains unported this phase, already ledgered separately), and — like `run_filter_tests` — the user-global filters tier is not consulted by the battery (only built-in + project, matching the oracle exactly). Covered by `VerifyCommandTests` (passing/failing/missing/summary scenarios) and `TomlFilterEngineTests` engine-level `RunFilterTests` tests. Wiring this battery surfaced a real pre-existing bug (not new to this task, but only exercised once the full battery actually ran end-to-end): Tomlyn preserves `\r\n` verbatim inside multi-line TOML strings, unlike Rust's `toml` crate, which normalizes newlines — so the 63 built-in filter `.toml` files (embedded resources checked out with CRLF under `core.autocrlf=true` on Windows) fed 15 spurious test failures (`139/154`) purely from stray `\r` bytes in test fixtures' `input`/`expected` literals. Fixed by normalizing `\r\n` → `\n` on the raw TOML text before every parse (`TomlFilterCompiler.ParseAndCompile`/`NormalizeNewlines` and `TomlFilterEngine.CollectTestOutcomes`), restoring `154/154 tests passed` (byte-exact with the oracle) and 100% `VerifyParityTests` parity. |
| `RewriteEngine`'s builtin transparent-prefix list includes `"uv run"` | Two *distinct* Rust constants exist: `SHELL_PREFIX_BUILTINS` (`src/core/config.rs:47` — `noglob`/`command`/`builtin`/`exec`/`nocorrect`, the 5-entry list a user's `transparent_prefixes` config *extends*) and `BUILTIN_TRANSPARENT_PREFIXES` (`src/discover/registry.rs:656-663` — the rewrite engine's own strip/recurse/re-prepend table), which is `"uv run"`, `noglob`, `command`, `builtin`, `exec`, `nocorrect` (6 entries). `rtk rewrite "uv run pytest tests/"` → `uv run rtk pytest tests/` on the oracle (`src/main.rs` inline test, line 2340). | `RewriteEngine.cs`'s `BuiltinTransparentPrefixes` is `["uv run", "noglob", "command", "builtin", "exec", "nocorrect"]` — a faithful port of `BUILTIN_TRANSPARENT_PREFIXES`, not `SHELL_PREFIX_BUILTINS`. | **Not a divergence — re-investigated in Phase 4 Task 5** after the task brief flagged `"uv run"` as a possible unreviewed addition (comparing it against the wrong Rust list, `SHELL_PREFIX_BUILTINS`). Confirmed against `src/discover/registry.rs:656-663`: the rewrite engine has always used its own 6-entry constant, and `"uv run"` is entry zero of *that* list, not an addition. Removing it would have been a regression (verified: `rtk rewrite "uv run pytest tests/"` returns `uv run rtk pytest tests/` on both the oracle and the port). Left unchanged; documented here per the task brief's instruction to ledger the investigation outcome rather than leave it silently unexplained. |
| `rtk config`/`rtk config --create` **`[tee]` section absent from RtkSharp's `config.toml`** | Rust's `Config` struct includes a `tee: TeeConfig` sub-config (`enabled = true`, `mode = "failures"`, `max_files = 20`, `max_file_size = 1048576`), which `show_config`/`Config::create_default` render as a `[tee]` section between `[filters]` and `[telemetry]` in both the `rtk config` stdout dump and the written `config.toml` file. | `RtkSharp/Core/Config.cs` has no `Tee` sub-config at all — `ToPrettyToml()` never emits a `[tee]` section, and the written `config.toml` file omits it entirely. | **Intentional, plan-sanctioned scope deferral, not a bug.** Phase 4 Task 1's brief explicitly instructed: "Rust's `Config` also has a `Tee` field — **skip it** for this task since `tee.rs` isn't ported yet"; the plan's Global Constraints section lists `tee.*` config as out-of-scope ("the tee/recovery store hasn't been ported to RtkSharp at all yet — separate phase"). Discovered concretely by Task 6's `ConfigFilterParityTests.cs` battery, which masks the oracle's `[tee]` section (via a scoped, deferral-only strip — not a general "ignore mismatches" escape hatch) from both the stdout comparison and the written-file SHA-256 manifest comparison before asserting parity, so the `rtk config`/`rtk config --create` entries measure everything Task 1 *did* port (all 6 implemented sub-configs, byte-exact) without either papering over or being blocked by the one sub-config Task 1 was told to omit. Revisit only if/when the tee/recovery store itself is ported in a future phase — at that point `Config` should gain a real `Tee` sub-config and this masking should be removed, not extended. |
| `rtk gain` default-view `check_rtk_disabled_bypass()` warning | Best-effort scan of the last 7 days of Claude Code session transcripts (via the `discover` module's `ClaudeProvider`/`cmd_has_rtk_disabled_prefix`) for `RTK_DISABLED=1` overuse; prints a `[warn] ... run \`rtk discover\` for details` line if the bypass rate exceeds 10% (`gain.rs:641-685`). Silent (returns `None`) on any error — missing session directory, unreadable file, etc. | Not ported. `GainCommand.cs`'s default view never calls an equivalent helper at all. | **Disclosed, in-scope-per-plan omission**, not a partial/fake implementation. Per the Phase 5 plan's Scope Decision, this sub-feature depends entirely on the `discover` module (`ClaudeProvider`, session-transcript parsing), which is out of scope this phase. Rust's own contract for this helper is already "best-effort, silent on any error" — a host with no readable Claude session directory already yields `None` (no warning) on the Rust side too — so RtkSharp never printing this one line is an in-contract degradation, not a behavior change from any state a real user would observe differently. Revisit only if/when the `discover` module is ported. |
| `rtk gain --tier`/`--yes` flag-`requires` violation error text | Clap renders a full multi-line usage error: `error: the following required arguments were not provided:\n  --quota\n\nUsage: rtk gain --quota --tier <TIER> ...\n\nFor more information, try '--help'.` (exact wording/wrapping depends on Clap's usage-string renderer and terminal width). | `GainCommand.cs`'s hand-rolled `GainArgs.Parse` emits a similarly-shaped but not byte-identical `error: the following required arguments were not provided:\n  --quota\n\nUsage: rtk gain --quota --tier <TIER>\n\nFor more information, try '--help'.` message, exits 2 (Clap's own usage-error exit code). | Deliberate scope-bounded simplification: re-implementing Clap's usage-string renderer (full arg-list layout, terminal-width wrapping) for a single command's two `requires` relationships is disproportionate effort for no behavioral benefit — the actually-observable contract (nonzero exit, no raw-command fallback, no partial stdout) is preserved exactly. Not byte-compared by any parity battery entry for this reason. |
| `rtk gain`'s `shorten_path`/`--project` scope path-component splitting | `Path::components()` iterates platform-specific components — on Windows, a distinct `Prefix` component (e.g. `C:`) then `RootDir`, then each `Normal` segment; further complicated by `resolve_project_scope`'s `canonicalize()` call, which prepends the Windows verbatim `\\?\` prefix. | `GainCommand.ShortenPath`/`SplitPathComponents` instead splits on `Path.GetPathRoot` plus a plain `/`/`\` segment split, and `ResolveProjectScope` uses `Path.GetFullPath` (no `\\?\` prefix) — the same canonicalization convention `Tracker.CurrentProjectPathString` already established in Task 2, so `--project`-scoped queries stay internally consistent with what `Tracker.Record` stores. | Intentional, effort-bounded simplification: agrees with Rust's component count/output for ordinary project paths (the overwhelmingly common case), but does not replicate the verbatim-path/prefix-component edge cases exactly. Revisit only if a real byte-exact parity failure is observed against a verbatim-path input. |
| `Tracker::cleanup_old()` hardcoded 90-day retention window | `cleanup_old()` (`tracking.rs:439-450`) deletes `commands`/`parse_failures` rows older than `now - DEFAULT_HISTORY_DAYS` where `DEFAULT_HISTORY_DAYS: i64 = 90` is a hardcoded constant (`constants.rs:6`) — it never reads `config.tracking.history_days`, despite that config field existing and being wired through TOML parsing on both sides. | `RtkSharp/Core/Tracking/Tracker.cs`'s private `CleanupOld()` uses `TrackingConstants.DefaultHistoryDays = 90` verbatim, likewise never consulting `Config.Tracking.HistoryDays`. | **Deliberate quirk replication, not a bug.** Per the Phase 5 plan's Global Constraints, this is one of the explicitly-called-out "known Rust quirks to replicate verbatim" — a user setting `history_days` in `config.toml` has zero effect on retention in either language, and RtkSharp intentionally does not "fix" this by wiring the config value in on the port's side only (which would itself be a parity divergence). `Tracker.cs`'s own doc remarks flag this at the call site. Revisit only if the Rust oracle itself starts honoring the config field. |
| `rtk cc-economics` / `rtk session` deferred to a later phase | `cc-economics` shells out to an external `ccusage`/`npx ccusage` process to report Claude Code subscription economics; `session` (and `gain`'s own `check_rtk_disabled_bypass()` helper, ledgered separately above) depend on the `discover` module (`ClaudeProvider`, `classify_command`, `split_command_chain`) to parse Claude Code session transcripts. | Neither is ported in Phase 5. `GainCommand.cs`/the command registry have no `cc-economics` or `session` verb at all. | **Explicit, plan-sanctioned scope deferral**, not an oversight. Per the Phase 5 plan's Scope Decision: `cc-economics` is not hermetically testable (it depends on shelling out to an external Node process that may not even be installed) and is lower MVP priority than `gain`; `session` depends entirely on the entirely-unported `discover` module, which is a separate, larger porting effort of its own. Both are listed in `docs/parity/command-inventory.md` as "port after core." Revisit once `discover` is ported in a future phase — `session` and the `check_rtk_disabled_bypass()` gap both become unblocked at the same time, since they share the same dependency. |
| **RESOLVED.** `rtk pipe`'s named-filter/auto-detect aliases (`cargo-test`/`cargo`, `pytest`, `go-test`, `go-build`, `tsc`, `vitest`, `log`, `mypy`, `ruff-check`, `ruff-format`, `prettier`) now delegate to their real ecosystem filters instead of `IdentityFilter` | `src/cmds/system/pipe_cmd.rs`'s `resolve_filter`/`auto_detect_filter` dispatch these aliases to the corresponding ecosystem filter module's real function (e.g. `cargo_cmd::filter_cargo_test`, `pytest_cmd::filter_pytest_output`, `vitest_cmd::VitestParser`, `go_cmd::filter_go_test_json`/`filter_go_build`, `tsc_cmd::filter_tsc_output`, `mypy_cmd::filter_mypy_output`, `ruff_cmd::filter_ruff_check_json`/`filter_ruff_format`, `prettier_cmd::filter_prettier_output`, `log_cmd::run_stdin_str`). | `PipeCommand.cs`'s `ResolveFilter`/`AutoDetectFilter` now call the real ported filters directly: `CargoBuildTestFilters.FilterCargoTest`, `PytestFilters.FilterPytestOutput`, `MypyFilters.FilterMypyOutput`, `RuffFilters.FilterRuffCheckJson`/`FilterRuffFormat`, `GoFilters.FilterGoTestJson`/`FilterGoBuild`, `TscCommand.FilterTscOutput`, a new `VitestWrapper` helper (parses via the shared `VitestCommand.VitestParser` then always renders `FormatMode.Compact`, mirroring Rust's own `vitest_wrapper` hardcoding compact mode regardless of verbosity), `PrettierCommand.FilterPrettierOutput`, and `LogCommand.AnalyzeLogs` (the `run_stdin_str` equivalent). | **Was disclosed-gap, now resolved as each ecosystem module was ported.** This row previously documented an intentional `IdentityFilter` passthrough for every alias above, written when none of those ecosystem modules existed in RtkSharp yet — per its own text, "Revisit each alias only once its own ecosystem module is ported... at that point `ResolveFilter`/`AutoDetectFilter` should be updated to delegate to it instead of `IdentityFilter`." All of Rust/Cargo, Python, Go, and JS are now ported, so this was done. `git-log`/`git-diff`/`git-status` and the two pipe-only mini filters (`grep`/`rg`/`find`/`fd`) were already genuinely delegating/ported and are unaffected. Verified end-to-end: `printf '...test result: ok. 3 passed...' \| rtk pipe --filter cargo-test` now prints `cargo test: 3 passed (1 suite, 0.01s)` instead of echoing the raw input. Test coverage in `RtkSharp.Tests/Commands/System/PipeCommandTests.cs` was rewritten from asserting identity-passthrough to asserting real delegation (e.g. `ResolveFilter_CargoTest_DelegatesToCargoBuildTestFilters`, one test per alias plus the auto-detect signature branches), full suite (3141 tests) green after the change. |
| **RESOLVED.** `rtk psql`/`rtk wget` — the two remaining "Defer"-classified commands — are now ported for full 68/68 command-inventory coverage | `src/cmds/cloud/psql_cmd.rs` (table/expanded-format compaction) and `src/cmds/cloud/wget_cmd.rs` (progress-bar stripping, compact one-line result, plus a `-O -` stdout-piping mode) | New `RtkSharp/Commands/Cloud/PsqlCommand.cs` (buffered via `CommandRunner.RunFilteredAsync` with `SkipFilterOnFailure`/`FilterStdoutOnly`/`TeeLabel: "psql"`, matching Rust's `RunOptions::stdout_only().tee("psql").early_exit_on_failure()`) and `WgetCommand.cs` (manual `IProcessExecutor` + `TimedExecution`, mirroring `main.rs`'s `Commands::Wget` dispatch: `-O -` routes to `RunStdoutAsync`, otherwise any `-O`/`--output-document` value is re-injected as `-O <file>` ahead of the remaining args before `RunAsync`). Both PASSTHROUGH-classified (absent from `RTK_META_COMMANDS`); neither throws `CommandArgumentParseException` for psql (its clap arm is a bare `trailing_var_arg` catch-all with no validation of its own), while wget's required `url` positional does throw it (matching `AwsCommand`'s established precedent for a required-positional PASSTHROUGH command). | Both were small enough to port directly rather than delegate to a subagent. 35 new tests (19 psql, 16 wget) port every Rust `#[cfg(test)]` case verbatim (fixture strings, token-savings-threshold assertions, filename-extraction edge cases including the `«»`/`'` French/English "Saving to" parsing and the URL-fallback path). `#[command(disable_help_flag = true)]` on `Psql` was confirmed to need no C# analog: RtkSharp's dispatch layer has no global help interceptor to disable in the first place, so a literal `--help`/`-h` token already flows through to the real `psql` binary unchanged. Full suite green after wiring (`psql`/`wget` registered in `CommandRegistry.cs`). |
| `rtk proxy`'s orphan-child-prevention signal handler (Unix-only, no Windows equivalent) | `main.rs`'s `Commands::Proxy` arm registers a `#[cfg(unix)]` `libc::signal` handler (SIGINT/SIGTERM) that kills the tracked child PID before re-raising the signal, guarding against orphaned children when `panic=abort` skips `ChildGuard`'s `Drop` impl (issue #897). This handler is compiled out entirely on Windows — the Rust source itself has no signal-handling equivalent there, relying solely on `ChildGuard::drop`-style best-effort cleanup. | `ProxyCommand`/`StreamingExecutor` port only the `ChildGuard`-equivalent best-effort disposal (kill+wait any still-running child on early exit/exception inside `StreamingExecutor`'s own try/catch) — no .NET signal handler of any kind is registered. | **Platform-scoped non-issue, not a gap.** RtkSharp's target platform for this phase is Windows, where the Rust oracle itself has no signal-handling code path to be byte-exact with — porting a .NET `PosixSignalRegistration`-based handler would be inventing behavior the Rust source doesn't have on this platform, not replicating it. Explicitly scoped out in the Phase 6 plan's Scope Decision. Revisit only if/when this port targets Unix, at which point the real `#[cfg(unix)]` handler becomes an actual porting target. |
| `rtk err`/`rtk test` absent from `RTK_META_COMMANDS` (unlike `run`/`proxy`/`pipe`/`gain`) | `main.rs`'s `RTK_META_COMMANDS` constant lists `run`, `proxy`, `pipe`, `gain` (among others) but not `err`/`test` — both are classified under Rust's own `PASSTHROUGH` set instead (per `main.rs`'s `test_every_subcommand_is_classified` test), meaning `err`/`test` go through the normal operational-command dispatch path (`is_operational_command` includes them, so they hit `hooks::integrity::runtime_check()`), not the meta-command fast path `run`/`proxy`/`pipe`/`gain` get. | `RtkSharp.Cli.CommandRegistry` registers `err`/`test` as normal dispatch entries (the same way `git`/`gh`/`dotnet` are registered) — a plain `Register("err", ErrCommand.RunAsync)`/`Register("test", TestCommand.RunAsync)`, deliberately *not* given the same meta-command-style guarantee `run`/`proxy`/`pipe`/`gain` get above it in the same file. | **Preserved verbatim per house rule, not "corrected."** This looks like it could be a Rust-side oversight (both are genuinely RTK-only commands, structurally similar to `run`/`proxy`), but the Phase 6 plan's Global Constraints explicitly calls this out as a known asymmetry to replicate as-is. `Program.cs` does not yet build an integrity/operational-vs-meta gate for any command (no such gate exists elsewhere in the port yet), so in practice this is currently a no-op distinction on the RtkSharp side — but the *registration shape* (normal entry, not meta-command fast path) still mirrors the Rust source's classification faithfully, so the port is ready to plug into a real integrity gate the same way Rust does, if/when one is built. See `ErrCommand.cs`'s class remarks for the full explanation. |
| `rtk run`'s non-signal-aware exit-code fallback (vs. `rtk proxy`'s signal-aware helper) | `main.rs`'s `Commands::Run` arm uses a plain `status.code().unwrap_or(1)` for its exit code — on Unix, a signal-killed child (no exit code, only a termination signal) silently falls back to exit code `1` with no diagnostic, unlike `Commands::Proxy`'s arm, which uses the signal-aware `exit_code_from_status` helper (`src/core/utils.rs:213`) to detect and report a signal-based termination distinctly. | `RunCommand.cs`'s `RunCoreAsync` returns `result.ExitCode` directly (mirroring the plain `unwrap_or(1)` fallback) rather than routing through `Utils.ExitCodeFromStatus` — the same helper `ProxyCommand.cs` calls. | **Deliberate, source-verified asymmetry — not an inconsistency to unify.** `RunCommand.cs`'s own class remarks flag this explicitly: this is not an oversight to "fix" into matching `proxy`'s behavior, since Rust's own two commands genuinely behave differently from each other on this exact point. On Windows (this port's current target platform) the distinction rarely manifests in practice (Windows process termination semantics differ from POSIX signals), but the *code path* faithfully preserves the Rust source's asymmetry rather than silently unifying the two commands' exit-code handling for consistency's sake. Revisit only if the Rust oracle itself unifies the two arms. |
| `jest` absent from `is_operational_command`'s whitelist (unlike `vitest`, which shares 100% of its execution path) | `main.rs`'s `is_operational_command` (lines 2552-2599) lists `Commands::Vitest { .. }` explicitly but has no corresponding `Commands::Jest { .. }` arm, even though both variants route through the exact same shared `vitest_cmd::run_test` function (`main.rs`:2047-2049) — confirmed directly against the current `is_operational_command` source in this checkout. This reads as a likely Rust-side oversight (structurally, jest is exactly as "operational" as vitest), but per house rule is replicated exactly, not "fixed". | `RtkSharp.Cli.CommandRegistry` registers `"vitest"` and `"jest"` as two entries both pointing at `VitestCommand`'s shared implementation (`RunVitestAsync`/`RunJestAsync`, discriminated by `TestFramework`), with no operational-vs-meta gate distinguishing them — because no such gate exists anywhere in RtkSharp yet (confirmed: no `IsOperationalCommand`-named type/method exists in the `RtkSharp` project as of Phase 8). | **Preserved verbatim per house rule; currently a no-op, forward-looking disclosure.** Same shape as the existing `err`/`test`-absent-from-`RTK_META_COMMANDS` row above: RtkSharp has not yet ported any operational-command/integrity gate for JS-stack commands, so this asymmetry has no observable effect in the port today. It is disclosed here so that if/when such a gate is ported in a future phase, `jest` is deliberately left off its whitelist (mirroring the oracle) rather than added "for consistency" with `vitest`. Revisit only if the Rust oracle itself adds `jest` to `is_operational_command`. |
| `rtk prisma generate`'s unused `output_path` variable | `filter_prisma_generate` (`src/cmds/js/prisma_cmd.rs`:174-231) scans for a line containing both `"node_modules"` and `"@prisma"` and stores its trimmed text in a local `output_path` — but the only use of that variable anywhere in the function is `!output_path.is_empty()`, gating whether a HARDCODED literal `"  • Output: node_modules/@prisma/client\n"` is appended. The actual detected line content is computed and then discarded; it never appears in the output under any circumstances. | `PrismaCommand.FilterPrismaGenerate` (`RtkSharp/Commands/Js/PrismaCommand.cs`) ports this exactly: it detects presence/absence of a matching line via the same two-substring check, but always emits the same hardcoded string when one is found, never the real detected text. | **Deliberate quirk replication, not a bug.** Disclosed in `PrismaCommand.cs`'s own class remarks (item 1) and covered by a dedicated regression test asserting the literal hardcoded string appears regardless of the actual detected line's content. Revisit only if the Rust oracle itself starts using the real detected path. |
| `rtk prisma migrate dev`/`migrate status`'s literal `"202"` substring search for migration names (Y2030-cliff assumption) | Both `filter_migrate_dev` (`prisma_cmd.rs`:245) and `filter_migrate_status` (`prisma_cmd.rs`:314) extract a migration name via `line.find("202")` — a literal three-character substring search, not a date parser — assuming Prisma's timestamp-prefixed migration folder names (`YYYYMMDDHHMMSS_name`) always start with the literal characters `"202"`. From the year 2030 onward this silently stops matching a migration name that no longer contains that substring at the expected position (and would spuriously match any unrelated line containing "202" elsewhere). | `PrismaCommand.FilterMigrateDev`/`FilterMigrateStatus` port the identical `line.IndexOf("202", Ordinal)` search verbatim, not "fixed" to be year-agnostic. | **Deliberate quirk replication, not a bug.** Disclosed in `PrismaCommand.cs`'s class remarks (item 2) and covered by regression tests. Revisit only if the Rust oracle itself replaces this with a real timestamp-prefix parser. |
| `rtk prisma migrate dev`'s always-zero pending-migration count | `filter_migrate_dev` (`prisma_cmd.rs`:296-299) prints the literal string `"Applied \| Pending: 0\n"` whenever any line contains `"applied"` or `"✓"` — the `0` is a hardcoded literal, not a computed pending-migration count; there is no pending-counting logic anywhere in the function. | `PrismaCommand.FilterMigrateDev` reports pending as literal `0` unconditionally in that branch, regardless of whatever the real migration state might be. | **Deliberate quirk replication, not a bug.** Disclosed in `PrismaCommand.cs`'s class remarks (item 3), exercised by this phase's `JsStackParityTests.cs` battery entry (`prisma migrate dev: always-zero pending count quirk`) and a dedicated unit regression test. Revisit only if the Rust oracle itself starts computing a real pending count. |
| `rtk prisma migrate status`'s Rust source can PANIC on a crafted input; the port deliberately clamps instead | `filter_migrate_status` (`prisma_cmd.rs`:304-336) computes `let end = line[pos..].find(|c| c.is_whitespace()).unwrap_or(20);` then slices `line[pos..pos + end]` — verified directly against this checkout's Rust source: when no whitespace follows the `"202"` match and `pos + 20` exceeds the line's actual byte length (e.g. a line like `"applied 202"`, `pos=8`, `line.len()=11`, so `line[8..28]` is requested), this is an out-of-bounds string slice, which panics in Rust (crashing the entire `rtk` process on that one migration-status line). | `PrismaCommand.FilterMigrateStatus` (`RtkSharp/Commands/Js/PrismaCommand.cs`) computes the same `end` value but then clamps it via `end = Math.Min(end, line.Length - pos)` before slicing — verified in the source, with an inline comment citing this exact divergence — so the equivalent input never throws, producing a truncated-but-safe `latestMigration` string instead of crashing. | **Intentional, disclosed divergence favoring "never crash the user's rtk invocation" over exact panic-parity.** This is the one place in Phase 8 where the port does NOT replicate a Rust bug verbatim — per the project's mandatory fallback-pattern rule (`.claude/rules/rust-patterns.md`: "a filter must never crash rtk over a malformed flag/input"), crashing the entire CLI on a single malformed migration-status line is strictly worse than a mildly-garbled-but-safe migration name. The observable behavior differs only on this narrow out-of-bounds input; all other `filter_migrate_status` behavior (applied/pending counts, the well-formed-input migration-name extraction, including the same Y2030-cliff `"202"` assumption above) is byte-identical. Revisit only if the Rust oracle itself is patched to avoid the panic (at which point this row can likely be closed as a non-issue). |
| `pnpm outdated` always exits `0`, ignoring the child's real exit code | `run_outdated` (`pnpm_cmd.rs`:420-467) never inspects `result.success()`/the child's exit code anywhere in the function and unconditionally returns `Ok(0)` — real `pnpm outdated` exits non-zero whenever any outdated package exists, which Rust deliberately papers over so the mere presence of outdated packages never looks like an rtk-level failure. | `PnpmCommand.RunOutdatedAsync` ignores `result.ExitCode` entirely and always returns `0`, matching exactly. | **Deliberate quirk replication, not a bug.** Disclosed in `PnpmCommand.cs`'s class remarks and exercised directly by this phase's `JsStackParityTests.cs` battery entry (`pnpm outdated: shared TokenFormatter, always exits 0 despite child's nonzero exit`, whose synthetic stand-in tool deliberately exits `1` to prove both the oracle and the port still report `0`). Revisit only if the Rust oracle itself starts propagating the real exit code. |
| `rtk tsc` — buffered capture vs. Rust's real streaming execution path (explicit architecture decision, Task 4) | Rust ships TWO parallel, intended-to-be-equivalent implementations: a real streaming `TscHandler`/`BlockStreamFilter` (`tsc_cmd.rs`:45-105) that `run()` actually executes when a user runs `rtk tsc`, emitting tsc's raw diagnostic lines as they stream plus a trailing one-line summary; and a buffered `filter_tsc_output` (`tsc_cmd.rs`:107-214) that Rust's own `run()` never calls at all — it exists solely for `#[cfg(test)]`, and every one of Rust's own six `filter_tsc_output` unit tests exercises this buffered path directly, never the live streaming one. | `TscCommand.ExecuteAsync` implements the buffered `FilterTscOutput` (the grouped-by-file, no-per-file-cap, top-codes-summary rendering Rust's own tests check) and drives it through `CommandRunner`'s existing captured-filter pipeline, deliberately NOT introducing a new tsc-specific streaming primitive into RtkSharp's execution layer. | **Explicit, documented architecture decision (not an oversight), confirmed to cause a REAL, observable divergence from the live oracle — verified empirically, not just reasoned about.** `TscCommand.cs`'s own class remarks document the buffered-vs-streaming choice and its rationale (buffered capture is behaviorally sufficient for what Rust's own test suite actually verifies, and avoids a new streaming primitive). This phase's `JsStackParityTests.cs` battery confirms directly against the real `target/release/rtk.exe` oracle that the two rendering styles genuinely differ byte-for-byte on realistic multi-file, multi-error-code output (both battery entries `tsc: grouped-by-file output, no per-file cap (5 files)` and `tsc: top-codes summary line (multiple distinct error codes)` are explicitly listed as expected mismatches in `JsStackParityTests.ExpectedMismatchLabels`, each pinned by exact label per the Phase 6 Task 7 convention — not silently excluded via a loose percentage threshold). Exit codes still match on both entries. Revisit if/when true block-streaming is ported to RtkSharp's execution layer (at which point these two battery entries should be expected to start matching, and `ExpectedMismatchLabels` updated accordingly, per that convention's "now-passing gap" failure mode). |
| `rtk hook-audit`'s reader compares against a UTC cutoff while the (unported) writer stamps local time | `filter_since_days` (`hook_audit_cmd.rs`:56-69) formats its `--since N` cutoff as `%Y-%m-%dT%H:%M:%SZ` (UTC) and compares it lexically against each entry's raw timestamp string, but the only writer of that format, `audit_log` (`hook_cmd.rs`:289-321, gated on `RTK_HOOK_AUDIT=1`), stamps entries with `chrono::Local::now()` and NO trailing `Z`/offset — a genuine, pre-existing inconsistency in the oracle between how entries are written and how they are later filtered, not something introduced by this port. | `HookAuditCommand.FilterSinceDays` ports the identical UTC-cutoff lexical-string comparison verbatim; `HookAuditCommand`'s own port of the writer side has not been undertaken in this phase (`hook-audit` was scoped as a pure reader — see its class remarks), so this row documents an inherited-from-Rust quirk that will become directly observable once/if a writer is ported, not a divergence between the two implementations today. | **Deliberate quirk replication, not a bug — and not yet independently reproducible on the .NET side since no writer exists yet.** Disclosed in `HookAuditCommand.cs`'s class remarks. Revisit if a future phase ports `hook_cmd.rs`'s `audit_log` writer: at that point, decide whether to replicate the local-time write path verbatim (full quirk parity) or flag a new divergence if the port's own writer used UTC instead. |
| `rtk hook-audit --since` flag-parse error text | Clap renders its own multi-line usage-error banner for an invalid/missing `--since` value (exact wording/wrapping depends on Clap's renderer and terminal width). | `HookAuditCommand.ParseArgs` emits a short single-line `error: ...` message and exits 2 (Clap's own usage-error exit code), matching the established `GainCommand`/`GainArgs.Parse` convention (see the `rtk gain --tier`/`--yes` row above) rather than reproducing Clap's banner byte-for-byte. | Same rationale as the `rtk gain` row above: the observable contract (nonzero exit, no raw-command fallback, no partial stdout) is preserved exactly; re-implementing Clap's usage-string renderer for one flag on one command is disproportionate effort for no behavioral benefit. Not exercised by any parity battery entry for this reason. |

| `rtk telemetry`'s network calls are permanently inert stubs, matching the shipped oracle exactly | `send_ping`/`send_erasure_request` (`telemetry.rs`/`telemetry_cmd.rs`) are gated on `option_env!("RTK_TELEMETRY_URL")`, a compile-time environment variable never set on the reference `target/release/rtk.exe` build (confirmed via a binary string scan — no telemetry URL/domain literal exists anywhere in the executable). `send_erasure_request` therefore always immediately fails with `"no telemetry endpoint configured"`, and the pre-dispatch `maybe_ping()` (called before every command) always no-ops. | `TelemetryCommand.RunForget` never attempts a real HTTP call — it hardcodes the exact "could not reach server: no telemetry endpoint configured" failure text the real oracle actually prints, rather than implementing a real `HttpClient` POST that would only ever fail identically against this oracle anyway. | **Deliberate, safety-motivated port decision, not a corner cut.** Implementing a real network call here would introduce a genuine risk of an actual outbound POST if the port were ever run against a differently-configured environment; the current oracle build makes this observably indistinguishable from a real (always-failing) attempt. Revisit only if the Rust oracle ever ships with a real compiled-in `RTK_TELEMETRY_URL` — at that point this row should be reopened as a genuine, disclosed divergence rather than closed silently. |
| `rtk telemetry forget`'s tracking-database path ignores `RTK_DB_PATH`/`config.tracking.database_path` | `run_forget` (`telemetry_cmd.rs`:132-135) computes the history-database path directly as `dirs::data_local_dir().join("rtk").join("history.db")`, never consulting `RTK_DB_PATH` or the config file — unlike every other tracking-database access point in the codebase (`Tracker::get_db_path`), which checks `RTK_DB_PATH` first. | `TelemetryCommand.RunForget` reuses `TrustCommand.ResolveDataDir()` + the `TrackingConstants` literals directly (mirroring the Rust call exactly) rather than routing through `Tracker.ResolveDbPath` (used everywhere else in the port). | **Deliberate quirk replication, not a bug.** A user who has redirected their tracking database via `RTK_DB_PATH` will find `rtk telemetry forget` deletes the wrong (default-location) file — a real, surprising-but-genuine oracle behavior, disclosed in `TelemetryCommand.cs`'s own class remarks and preserved verbatim rather than "fixed" to honor `RTK_DB_PATH` for consistency. |
| `rtk telemetry`'s missing/unrecognized-subcommand and `--since`-style flag-parse error text | Clap renders its own multi-line usage-error banner for a missing required subcommand or an unrecognized one (exact wording/wrapping depends on Clap's renderer and terminal width). | `TelemetryCommand.Run` emits a short single-line `error: ...` message and exits 2 (Clap's own usage-error exit code), matching the established `GainCommand`/`HookAuditCommand` convention (see those rows above) rather than reproducing Clap's banner byte-for-byte. | Same rationale as the `rtk gain`/`rtk hook-audit` rows above: the observable contract (nonzero exit, no raw-command fallback, no partial stdout) is preserved exactly; re-implementing Clap's usage-string renderer for this one command is disproportionate effort for no behavioral benefit. Not exercised by any parity battery entry for this reason (the battery does assert the exit code, just not Clap's exact banner text). |

| `rtk session`'s `ExtractedCommand.OutputLen`/`OutputContent` use UTF-16 code units, not UTF-8 bytes | Rust's `ExtractedCommand.output_len` (`discover/provider.rs:226`) is the tool-result content's UTF-8 byte length, and its 1000-char preview (`provider.rs:234`) is 1000 Unicode scalar values (`chars().take(1000)`). | `ClaudeProvider.ExtractCommands` (`RtkSharp/Discover/ClaudeProvider.cs`) uses .NET `string.Length` (UTF-16 code units) for both `OutputLen` and the 1000-character preview truncation. | Both units only diverge for non-ASCII tool-result content, and `OutputLen` is only ever summed into `rtk session`'s "Output" column via `Utils.FormatTokens` — a rough token-count display, not a byte-exact metric. `OutputContent` (the preview) is not read by any current caller (`rtk session` never displays it). Revisit if a future feature consumes `OutputContent` for something byte/rune-sensitive. |
| `rtk session`'s Claude Code project-directory scan is case-insensitive for `.jsonl` and follows directory symlinks, unlike the Rust oracle | Rust's `discover_sessions_in_projects_dir` (`provider.rs:90-98`) checks `file_path.extension() == Some("jsonl")` (case-sensitive) via `WalkDir::new(&path).follow_links(false)` (never follows symlinked subdirectories). | `ClaudeProvider.DiscoverSessionsInProjectsDir` uses `Directory.EnumerateFiles(projectDir, "*.jsonl", SearchOption.AllDirectories)`, which matches case-insensitively on Windows and traverses directory symlinks/junctions by default. | Real Claude Code session files are always lowercase `.jsonl` with no symlinked subdirectories in practice, so this is inert for the port's actual use case. Disclosed for completeness rather than "fixed" with a bespoke case-sensitive, symlink-avoiding directory walker for a divergence with no realistic trigger. |

| `rtk lint ruff`/`rtk lint mypy` are unfiltered raw passthrough, not grouped output | `lint_cmd.rs` imports `crate::ruff_cmd`/`crate::mypy_cmd` and calls `ruff_cmd::filter_ruff_check_json`/`mypy_cmd::filter_mypy_output` directly for these two linter names — real, working filters compiled into the oracle. | Neither `ruff_cmd` nor `mypy_cmd` has been ported to RtkSharp (no Python-ecosystem work exists yet in this project — the same class of hidden dependency `rtk session` had on `discover`). `LintCommand.RunCoreAsync` still constructs the correct child-process invocation for `ruff`/`mypy` (arg parsing, `--output-format`/JSON flag injection, default-path logic are all generic and shared with `eslint`/`pylint`), but prints the raw combined stdout+stderr unfiltered for these two linter names rather than fabricating a plausible-but-unverified grouped format. | **Deliberate scope cut, disclosed in `LintCommand.cs`'s class remarks** — matches the Phase 8 "genuinely unimplemented ecosystem, don't fake it" convention already applied to `next`/`prettier`/`biome`. Revisit if/when the Python ecosystem (`ruff`, `pytest`, `mypy`, `pip`) gets its own porting pass — `ruff_cmd::filter_ruff_check_json`/`mypy_cmd::filter_mypy_output` would need to be ported first, then `rtk lint`'s dispatch updated to call them. |
| `rtk lint ruff`/`rtk lint pylint`'s `--output-format` handling silently drops the flag (and, for the two-token form, leaves a stray positional) when the user supplies it explicitly | `lint_cmd.rs`'s per-arg forwarding loop (lines 141-150) unconditionally skips any token starting with `--output-format` for `ruff`/`pylint` — with NO check for whether RTK itself actually injected a format flag on this invocation. The injection guard itself (lines 112,116) only tests for the *bare* `"--output-format"` token via `Vec::contains`, which never matches an `=`-joined `"--output-format=X"` form. Net effect, verified directly against `target/release/rtk.exe`: `rtk lint ruff --output-format text` invokes the child as `ruff text` (flag dropped, "text" becomes a stray positional, no format flag reaches ruff at all); `rtk lint pylint --output-format=text` invokes the child as `pylint --output-format=json2 .` (RTK's own default format wins anyway, since the `=` form didn't suppress injection, and the user's token is then dropped by the same unconditional skip). | `LintCommand.RunCoreAsync` reproduces both code paths verbatim — the injection-guard's exact-token `Contains` check and the unconditional `StartsWith("--output-format")` skip in the forwarding loop. | **Deliberate quirk replication, not a bug.** Both behaviors were independently re-derived from source, then confirmed byte-exact against the real oracle before being encoded as unit tests (`LintCommandTests.RunCoreAsync_RuffWithExplicitTwoTokenOutputFormat_DropsFlagButLeavesStrayValue`/`RunCoreAsync_RuffWithExplicitEqualsFormOutputFormat_StillInjectsJson`/`RunCoreAsync_PylintWithExplicitEqualsFormOutputFormat_StillInjectsJson2AndDropsUsersFlag`) rather than assumed from reading the source alone — an earlier draft of these tests asserted the "sensible" (wrong) behavior and was caught and corrected via direct oracle verification. A user who explicitly passes `--output-format` to `rtk lint ruff`/`rtk lint pylint` gets a different, and for the two-token ruff case genuinely broken, result — this is a real oracle limitation preserved as-is. |

| `rtk docker logs` with zero args exits 2 with a clap-style usage error, not exit 0 with the module's own usage line | `main.rs`'s `DockerCommands::Logs { container: String }` (line 920) declares `container` as a REQUIRED clap positional. `rtk docker logs` with no arguments never reaches `container.rs`'s own `run_logs` function at all — clap itself rejects the invocation before any RTK code runs, printing its standard "required arguments were not provided" banner and exiting 2. `container.rs:309`'s own `if container.is_empty()` usage-message branch is reachable only via an explicit empty-string argument (`rtk docker logs ""`), which clap does not reject since an empty string is still a valid value for a required positional. | `DockerCommand.RunLogsAsync` distinguishes the two cases: zero args (`rest.Length == 0`) emits a clap-style `"error: the following required arguments were not provided..."` message to stderr and returns exit code 2 (matching the established `GainCommand`/`HookAuditCommand`/`TelemetryCommand` short-usage-error convention already used elsewhere in this port for clap-level argument errors); an explicit empty-string container still hits the module's own `"Usage: rtk docker logs <container>\n"` stdout message with exit 0. | **Deliberate two-path replication, not a bug.** Found during independent review of the initial single-path implementation (which returned exit 0 for both cases) and confirmed against `main.rs`'s clap declaration. Covered by `DockerCommandTests.RunCoreAsync_Logs_NoArgsAtAll_UsageErrorExitTwo` and `RunCoreAsync_Logs_ExplicitEmptyStringContainer_PrintsModuleUsageMessage`. |
| `rtk docker ps`/`rtk docker ps -a` tie-break ordering for equal-count unique errors/warnings is more deterministic in RtkSharp than in the Rust oracle | `log_cmd.rs`'s `analyze_logs` collects unique error/warning lines into a `std::collections::HashMap`, whose iteration order is randomized per-process (Rust's `HashMap` uses `SipHash` with a random seed by default) before a stable sort by count; for lines tied on count, the oracle's own output order is not reproducible run-to-run. | `LogCommand.AnalyzeLogs` uses a `Dictionary<TKey,TValue>`, which preserves first-insertion order; for count-tied lines, RtkSharp's output order is therefore deterministic (always first-seen-first) where the oracle's is not. | **Property of the spec itself, not a port defect.** Since the Rust oracle's own tie-break order is unstable, there is no single "correct" order to match — RtkSharp's deterministic behavior is a strict improvement and was left as-is rather than artificially randomized to chase an unstable target. Noted during independent review of the `rtk log`/`rtk docker` port; no fix applied. |

| `rtk read --level minimal`/`--level aggressive` now recognize `Language::CSharp` for `.cs` files, an enum case Rust's `Language` does not have | `filter.rs`'s `Language::from_extension` has no `"cs"` arm at all — the oracle falls through to `Language::Unknown` for `.cs` files. | `RtkSharp/Core/SourceFilter.cs`'s `LanguageExtensions.FromExtension` maps `"cs"` to the new `Language.CSharp` case; `CommentPatterns()` groups `CSharp` with `Unknown` specifically (`("//", "/*", "*/", null, null)`), NOT with the JS/TS/Go/C/Cpp/Java family (which share the same line/block markers but differ in `DocBlockStart`: `"/**"` there vs. `null` for `Unknown`/`CSharp`). | **Inert addition, not a behavioral divergence — but only after a real bug was caught and fixed.** An initial version grouped `CSharp` with the JS/TS/Go/C/Cpp/Java arm (which also shares its `//`/`/*`/`*/` markers) rather than with `Unknown`, giving it `DocBlockStart = "/**"` instead of `Unknown`'s `null`. `DocBlockStart` is load-bearing in `MinimalFilter`'s block-comment-start guard (`!trimmed.StartsWith(patterns.DocBlockStart ?? "###", ...)`), so a `.cs` file containing a `/**`-prefixed block comment would have been *kept* by the port while the real oracle (which sees `.cs` as `Unknown`) *strips* it — a genuine byte-level divergence the original "inert" claim did not hold for. Caught by independent review (the original regression test's `/* block */` fixture didn't start with `/**`, so it passed even with the bug present) and fixed by grouping `CSharp` with `Unknown` explicitly instead of the JS family. Now verified by `SourceFilterTests.MinimalFilter_CSharp_SameCommentPatternsAsUnknown` (generic markers) AND `MinimalFilter_CSharp_JavaDocStyleBlockComment_StrippedSameAsUnknown` (the specific `/**` case that exposed the bug). The only remaining difference between `CSharp` and `Unknown` is the enum member's name, which nothing in the ported `read` command surfaces (verbose language-detection diagnostics are out of scope, per `ReadCommand.cs`'s class remarks). |
| `File.ReadAllText` silently strips a leading UTF-8 BOM; Rust's `fs::read_to_string` does not | Rust's `fs::read_to_string` returns the BOM's 3 bytes as a literal `U+FEFF` character at the start of the string — it performs no BOM detection/stripping at all. .NET's `StreamReader` (which `File.ReadAllText` uses internally) auto-detects and strips a UTF-8/UTF-16 BOM by default. | This is a **pre-existing, project-wide gap, not something introduced by the `--level minimal`/`aggressive`/`ast` work** — `File.ReadAllText` is already used 33+ times across 15+ files in `RtkSharp/` (including `ReadCommand.cs`'s own `--level none` path, which predates this filter-level work entirely). No shared BOM-preserving file-reading helper currently exists (`HookCommand.cs`'s `TryReadStdin` handles stdin only, via `detectEncodingFromByteOrderMarks: false`). | **Known, disclosed, out-of-scope-for-this-pass limitation.** Surfaced during independent review of the filter-level port, which correctly noted this affects `rtk read`'s byte-exactness for BOM-prefixed files (common for Visual Studio-authored `.cs` files) but is not specific to anything this pass changed. Fixing it properly means introducing a shared BOM-preserving read helper and auditing all 33+ call sites — a project-wide undertaking explicitly deferred rather than folded into this task's scope. Revisit as its own dedicated pass if BOM-prefixed-file fidelity becomes a real user-facing complaint. |
| `rtk read --level aggressive` on C# source reuses the exact same `IMPORT_PATTERN`/`FUNC_SIGNATURE` regexes as every other language, so C# idioms trip the same known heuristic gaps Java/JS already have — `using System;` is dropped (not recognized as an import), and `public class Foo {` is not recognized as a signature (only the bare `class Foo {`, without the `public`/`internal` modifier, matches) | `filter.rs`'s `IMPORT_PATTERN` requires a literal `"use "` prefix (Rust's own keyword) — C#'s `using` doesn't match. `FUNC_SIGNATURE` requires one of `fn/def/function/func/class/struct/enum/trait/interface/type`, optionally prefixed by literal `"pub "` (Rust's own visibility keyword) — C#'s `public`/`internal`/`private` modifiers don't match that literal prefix, and `void`/method return types aren't in the keyword list at all, so only bare type-declaration lines (`class`/`struct`/`enum`/`interface`) are ever recognized as "signatures" for C#, never method signatures. | `RtkSharp/Core/SourceFilter.cs`'s `AggressiveFilter` reuses the identical two regexes verbatim for every language, `CSharp` included — no C#-specific pattern was added, per the explicit instruction to extend the *existing* heuristic rather than build a smarter one. | **Deliberate, disclosed limitation, not a bug** — this is the natural consequence of reusing a heuristic designed around Rust/Python/JS/Go keyword shapes for a language it was never written for, and the exact same gap already exists for Java's `public class Foo`/`public void bar()` today in the real Rust oracle. Traced by hand against the algorithm and confirmed by running the actual port (`SourceFilterTests.AggressiveFilter_CSharp_DropsUsingDirective_KeepsBareClassSignature`) since no Rust oracle exists for `.cs` files to cross-check against. If C# aggressive-filtering quality matters later, the fix is a smarter, C#-aware pattern set — explicitly out of scope for this pass, which only extends language *coverage*, not heuristic *quality*. |
| `rtk read --level minimal` on a single-line Python docstring (both `"""` delimiters on the same line) leaves docstring mode "stuck on" for the rest of the file — every subsequent line, including real comments, is kept verbatim instead of being comment-stripped | `filter.rs`'s `MinimalFilter::filter` toggles `in_docstring = !in_docstring` once per line whenever `trimmed.starts_with("\"\"\"")` (line ~186), with no same-line-close detection — a line like `"""Module docstring."""` toggles the flag on and never back off within that same line, so it stays on until some *other* later line happens to start with `"""` to toggle it off again (or end of file, whichever comes first). | `RtkSharp/Core/SourceFilter.cs`'s `MinimalFilter.Filter` reproduces the identical single-toggle-per-line logic. | **Faithfully preserved genuine oracle bug, not "fixed."** Verified directly against `target/release/rtk.exe read --level minimal` on a single-line-docstring fixture — the oracle really does keep a subsequent `# comment` line verbatim. Covered by `SourceFilterTests.MinimalFilter_Python_SingleLineDocstringLeavesDocstringModeStuckOn`. This was caught while writing tests for the ported `filter.rs` engine (first port of this module) — an initial test draft assumed proper open/close docstring handling and had to be corrected after direct oracle verification, consistent with this project's established "verify surprising behavior against the live oracle" discipline. |
| `rtk read`'s file-extension detection for dotfiles (`.gitignore`, `.env`, etc.) follows Rust's `Path::extension()` semantics, not .NET's `Path.GetExtension` | Rust's `Path::extension()` returns `None` when the file name begins with `.` and has no *other* `.` (so `.gitignore`/`.env` have no extension, falling through to `Language::Unknown`); `.env.local` DOES have an extension (`local`), since there's a second `.`. .NET's `Path.GetExtension(".env")` instead returns the whole string `".env"` — naively stripping the leading dot would give `"env"`, which this project's `from_extension` match maps to `Language::Data`, silently misclassifying `.env` files (skipping their C-style comment stripping) where the real oracle treats them as `Unknown` (and DOES C-style-comment-strip them). | `ReadCommand.GetExtension` reimplements Rust's exact rule (empty when the last `.` index is `<= 0`) instead of calling `Path.GetExtension` directly. | **Bug avoided by using Rust's exact semantics, not .NET's.** Found while porting language detection for the new filter-level support, before it ever shipped — not a live divergence. Covered by `ReadCommandTests.GetExtension_MatchesRustPathExtensionSemantics` (`.gitignore`/`.env` → empty, `.env.local` → `local`) and `RunAsync_AggressiveLevel_OnDotfileWithNoExtension_TreatedAsUnknownNotData`. |
| `rtk read --level ast` is a wholly new, RtkSharp-only filter tier with no Rust counterpart at all | Rust's `filter.rs` defines exactly `None`/`Minimal`/`Aggressive` — there is no fourth tier, and no AST/semantic parsing anywhere in the Rust codebase (confirmed: no `tree-sitter`/`syn`/`ra_ap_*` or similar dependency in `Cargo.toml`). | `RtkSharp/Core/SourceFilter.cs`'s `FilterLevel.Ast` plus the new `RtkSharp.Ast` namespace (`IAstAnalyzer`, `AstAnalyzerRegistry`, `AstFilter`, `CSharpAstAnalyzer`). `FilterLevelParser.Parse` (which mirrors `FilterLevel::from_str` exactly) deliberately does NOT recognize `"ast"` — only `ReadCommand`'s own `--level` CLI parser does, as an RtkSharp-specific superset. | **Deliberate net-new capability beyond the ported spec, not a parity gap.** Pluggable by design: `AstAnalyzerRegistry` maps `Language` → `IAstAnalyzer`; a language with no registered analyzer falls back to the existing regex-based `AggressiveFilter` with a stderr note, so `--level ast` never hard-fails for a language it doesn't yet understand. C# is the reference implementation, using Roslyn's syntax-tree API (`Microsoft.CodeAnalysis.CSharp`) at the syntax layer only (no `CSharpCompilation`/semantic model/metadata references) to keep it a dependency-free, per-file text transform. Because Rust has no `--level ast` at all, there is no oracle to diff against for correctness here — correctness is judged against the parsed structure itself (does the summary faithfully represent what was actually parsed?), verified by the analyzer's own unit tests (`CSharpAstAnalyzerTests`), not by CLI-to-CLI comparison; accordingly, `--level ast` is deliberately NOT added to `SystemParityTests`' oracle-comparison battery (see the entry below for why the oracle rejects it outright anyway). Real, measured cost of adding Roslyn: published Native-AOT binary size grew from ~12.3 MB to ~21.6 MB (+75%, confirmed via a real `dotnet publish -p:PublishAot=true` smoke test of the actual published executable, not just a successful build); process startup time was unaffected within measurement noise (~130 ms warm both before and after — both already well above this project's aspirational <10ms target at baseline, so this feature did not cause that pre-existing gap). |
| `rtk read --level ast` (a value the oracle's clap parser has never heard of) now correctly falls back like any other unrecognized `--level` value, per the RESOLVED entry below — no separate divergence to track | Same mechanism as any other clap-rejected `--level` value. | `ReadCommand.ParseLevel` recognizes `"none"`/`"minimal"`/`"aggressive"`/`"ast"` and produces a genuine result for all four; `ParseArgs`/its helpers throw `CommandArgumentParseException` for anything else, which now correctly falls back (see below) rather than producing a bespoke exit-2 message. | **No longer a distinct divergence.** Superseded by the general fix below — recorded here only so a reader following this ledger's history doesn't wonder whether `--level ast` still has its own special-cased error shape. |
| **RESOLVED (was: MAJOR, PRE-EXISTING).** Any `rtk read`/`rtk docker`/`rtk prisma` invocation that fails clap-equivalent argument parsing now correctly falls back to the TOML-filter-lookup/raw-PATH-passthrough mechanism, matching the real oracle, instead of printing a command-specific usage error | `main.rs`'s `run_cli()` (~line 1472) calls `Cli::try_parse()`; on ANY parse error other than `DisplayHelp`/`DisplayVersion`, it calls `run_fallback(e)` (~line 1193) — a generic mechanism, not specific to any one subcommand, that re-resolves and re-execs the ENTIRE original argv as a raw external command (after a TOML-filter-lookup attempt) whenever clap can't make sense of it — UNLESS the subcommand is in the fixed `RTK_META_COMMANDS` allowlist (`main.rs:1170`: `gain`, `discover`, `learn`, `init`, `config`, `proxy`, `run`, `hook`, `hook-audit`, `pipe`, `cc-economics`, `verify`, `trust`, `untrust`, `session`, `rewrite`, `telemetry`, `smart`, `deps`, `json`), in which case a parse failure exits via clap's own error instead — this exemption is enforced by `main.rs`'s own test `test_every_subcommand_is_classified`, which requires every subcommand to be in exactly one of `RTK_META_COMMANDS` or `PASSTHROUGH` (the latter, `main.rs:2925`, is the much longer list of commands that wrap a real external tool and whose parse failures DO fall back). | New `RtkSharp/Cli/CommandArgumentParseException.cs` — thrown by a registered command's own argument parser to signal a clap-equivalent parse failure. `RtkSharp/Program.cs`'s `RtkProgram.RunAsync` catches it around the registered-handler call and re-dispatches through the SAME `TryTomlFallbackAsync`/raw-passthrough path already used for a completely unregistered verb — one mechanism, two trigger paths. Migrated the three PASSTHROUGH-classified registered commands whose own C# code reproduced a clap-layer rejection: `ReadCommand.ParseArgs` (unknown flag, missing option value, invalid `--level`/count value, conflicting `--max-lines`/`--tail-lines`, missing required files), `DockerCommand` (zero-args `docker` requiring a subcommand; `docker logs` with zero args requiring `<CONTAINER>`), `PrismaCommand` (missing/unknown top-level and `migrate`-level subcommands). Every other currently-registered command was independently re-derived against Rust's `PASSTHROUGH` list during two rounds of review and confirmed to either forward args verbatim with no validation of its own, or do its parsing inside what would be the Rust subcommand's own *body* (post-clap, e.g. `find`'s/`dotnet`'s/`npx`'s anyhow-style bails, which Rust reaches identically) — so no other command needed migrating. | **Verified against the real oracle, not assumed.** `read`/`prisma` (no real same-named binary on PATH) now correctly exit 127 with the oracle's own "Binary not found" fallback shape; `docker` (a REAL binary usually present) now correctly falls all the way through to running the actual `docker` CLI, surfacing ITS OWN output and exit code (`docker` with no subcommand: real docker's help text, exit 0; `docker logs` with no container: real docker's `'docker logs' requires 1 argument`, exit 1) — this was a genuinely wrong assumption in the ORIGINAL docker port's own commit message, which assumed a clean clap-style exit 2 was the correct oracle behavior without checking whether `docker` was classified `RTK_META_COMMANDS` (it is not). Two real bugs were found and fixed while migrating: (1) `DockerCommand.RunAsync`'s and `PrismaCommand.RunPrismaSafeAsync`'s own outer `catch (Exception ex)` safety nets were silently swallowing the new exception before it ever reached `Program.cs` — fixed with `when (ex is not CommandArgumentParseException)` on both; (2) the same review then found this exact swallow-pattern latent (not yet triggered, since none of them throw the exception today) in eleven OTHER registered commands' entry wrappers (`playwright`, `npm`, `npx`, `lint`, `pnpm`, `tsc`, `vitest`/`jest`, `err`, `test`, `curl`, `log`) — all eleven were proactively guarded the same way so a future migration of any of them can't silently regress into the same bug. A related, genuine, PRE-EXISTING divergence was also found and fixed during the same review: `EnvCommand.ParseArgs` silently ignored any unrecognized flag or stray positional instead of rejecting it (Rust's `Env { filter: Option<String>, show_all: bool }` has no positional field and no `trailing_var_arg`, so any such token is a clap-layer failure) — `env` IS a real, always-present binary, so this was a live, exercisable divergence (RtkSharp printed filtered env output with exit 0; the oracle falls back to running real `env`, exit 125/126/127 depending on the exact failure) — fixed the same way, verified byte-exact against the oracle (`env --badflag` → `env: unknown option -- badflag`, exit 125; `env somepositional` → `/usr/bin/env: 'somepositional': No such file or directory`, exit 127). New coverage: `ReadCommandTests`/`DockerCommandTests`/`PrismaCommandTests`/`EnvCommandTests` unit-level throw assertions, true end-to-end `RtkProgram.RunAsync`-driven integration tests for `read`/`docker`/`env` (confirming the real fallback dispatch, not just that an exception is thrown), and two new `DockerParityTests` entries that spawn both the real oracle and RtkSharp against a synthetic PATH `docker` stub to prove the re-exec genuinely happens. |

| `rtk read --level ast` now has real AST analyzers for 10 languages (C#, Rust, Python, JavaScript, TypeScript, Go, C, C++, Java, Ruby), backed by `TreeSitter.DotNet` (native tree-sitter bindings) for 9 of them and `Acornima` (pure-managed) for JavaScript specifically — a wholly new capability, no Rust equivalent exists at all | Rust's `filter.rs` has no `ast` level, no semantic/AST parsing, and no per-language plugin concept — `--level ast` and every analyzer under it are pure RtkSharp additions. | `RtkSharp/Ast/AstAnalyzerRegistry.cs` registers one `IAstAnalyzer` per language: `RustAstAnalyzer`, `PythonAstAnalyzer`, `JavaScriptAstAnalyzer`, `TypeScriptAstAnalyzer`, `GoAstAnalyzer`, `CAstAnalyzer`, `CppAstAnalyzer`, `JavaAstAnalyzer`, `RubyAstAnalyzer` (all `TreeSitter.DotNet`), plus the pre-existing `CSharpAstAnalyzer` (Roslyn). `ShellAstAnalyzer` (`TreeSitter.DotNet`, grammar `Bash`) is also registered but deliberately narrow-scoped — see its own class remarks — since shell syntax is too fragile to safely restructure beyond collapsing recognized function bodies. | **Deliberate net-new capability, not a parity gap** — there is no oracle for any of this, so correctness is judged per-analyzer against the real parsed structure (verified by each analyzer's own test suite: Rust 21, Python 25, JavaScript 28, TypeScript 24, Go 24, C 26, C++ 25, Java 21, Ruby 23, Shell 21 — 238 tests total), not by CLI-to-CLI comparison; none of these languages are added to `SystemParityTests`' oracle-comparison battery for the same reason `--level ast` itself isn't (see the entry above). Two real bugs were found and fixed during independent review of the parallel implementation batch: `CAstAnalyzer` and `ShellAstAnalyzer` both computed a header-text boundary using tree-sitter's native **byte** offsets and applied that delta as a .NET **char** count, silently mis-slicing (corrupting or leaking a stray brace into) any signature preceded by a multi-byte UTF-8 character — fixed via UTF-16-safe suffix removal (C, matching the pattern already used correctly by `GoAstAnalyzer`/`CppAstAnalyzer`) and `Tree.GetText(byteStart, byteEnd)` (Shell, since a shell function can be followed by redirections after its closing brace, making suffix removal unsafe there specifically). Both fixes are covered by new non-ASCII regression tests. Measured deployment cost (original): publishing with `PublishAot=true` was re-verified end-to-end after wiring in all 9 new analyzers (the published executable was actually run against real fixtures per language, not just built) — the executable itself grew only marginally (~21.6 MB → ~22.3 MB, since `TreeSitter.DotNet`'s managed layer and `Acornima` are both small/pure-managed), but the **total distributable footprint grew substantially to ~153 MB** because `TreeSitter.DotNet` bundles 28+ native grammar DLLs (one per supported language, ~1-18 MB each) alongside the executable regardless of how many are actually used by RtkSharp's own registry, and the package doesn't offer a way to select a subset of grammars at restore time as of `TreeSitter.DotNet` 1.3.0. **Since resolved**: RtkSharp now depends on `RtkSharp.TreeSitter.Trimmed` (see `third-party/treesitter-dotnet-trimmed/README.md`) — a size-trimmed, MIT-licensed redistribution of the same upstream binaries, keeping only the 9 grammars RtkSharp's registry actually loads (`c`, `cpp`, `go`, `java`, `python`, `ruby`, `rust`, `bash`, `typescript`; C# is Roslyn-based and JavaScript is Acornima-based, so neither ever touched `TreeSitter.DotNet`). Measured after the swap: native payload per RID drops from ~68 MB (31 files) to ~13.6 MB (10 files) for `win-x64`; re-verified with a real `PublishAot=true` publish + run against Rust/Python/JavaScript fixtures, all green, plus the full 2172-test suite. |
| `rtk cargo` — final "Must port for MVP" command now ported: `build`/`test`/`check` stream via a newly-ported `BlockHandler`/`BlockStreamFilter` pair; `clippy`/`install`/`nextest` reuse the existing buffered `CommandRunner` pipeline; anything else is raw passthrough | `src/cmds/rust/cargo_cmd.rs` (2217 lines): `CargoBuildHandler`/`CargoTestHandler` implement Rust's `BlockHandler` trait, driven by `core::stream::BlockStreamFilter` over `runner::run_streamed` for `build`/`test`/`check`; `filter_cargo_clippy`/`filter_cargo_install`/`filter_cargo_nextest` are buffered (non-streaming) functions driven via `runner::run_filtered`; `run_passthrough` execs any other cargo subcommand unfiltered via `stream::run_streaming(StdinMode::Inherit, FilterMode::Passthrough)`. All six filtered subcommands restore a stripped `--` via `args_utils::restore_double_dash`. | New `RtkSharp/Core/IBlockHandler.cs` + `RtkSharp/Core/BlockStreamFilter.cs` (the first C# port of Rust's generic `BlockHandler`/`BlockStreamFilter<H>` pair — introduced here, reused by both `CargoBuildHandler` and `CargoTestHandler`, per the project's DRY convention for 2+ similar instances). New `RtkSharp/Commands/Rust/CargoCommand.cs` (dispatch + `build`/`test`/`check` via `LineFilteringExecutor`, mirroring `ErrCommand.cs`'s streaming-dispatch shape) + `CargoBuildTestHandlers.cs` (`CargoBuildHandler`, `CargoTestHandler`, `AggregatedTestResult`, buffered `FilterCargoBuild`/`FilterCargoTest` equivalents) + `CargoNonStreamingFilters.cs` (`FilterCargoClippy`/`FilterCargoInstall`/`FilterCargoNextest`/`FormatCrateInfo`, driven through the pre-existing `CommandRunner.RunFilteredAsync` — Rust's `runner::run_filtered` was already ported generically for other filters, so it is reused as-is rather than reimplemented). `--` restoration reuses the already-ported `GitCommand.RestoreDoubleDashWithRaw` (same underlying algorithm as `args_utils::restore_double_dash_with_raw`) rather than adding a third duplicate copy. Registered in `RtkSharp/Cli/CommandRegistry.cs` as a normal `PASSTHROUGH`-classified dispatch entry (`cargo` is confirmed in Rust's own `PASSTHROUGH` list, `main.rs`'s `test_every_subcommand_is_classified`, not `RTK_META_COMMANDS`). | **Faithfully ported, verified against real `cargo` output (including a real, if broken by an unrelated local toolchain issue, `rtk cargo test -- --nocapture` invocation confirming exactly one `--`, correctly positioned).** Every Rust-source quirk was preserved verbatim, not "improved": (1) **`cargo check` reports as "cargo build"** — Rust's `run_check` drives the literal same `CargoBuildHandler` as `run_build`, whose `format_summary` always emits `"cargo build (...)"`/`"cargo build: ..."` text, never `"cargo check"`; this looks like an oversight but is preserved as-is (`CargoBuildHandler`'s class remarks flag it explicitly). (2) **The second `failures:` line in `cargo test` output is a bare name-listing section, silently discarded** — cargo prints `failures:` twice per run (once introducing per-test detail blocks, once introducing a trailing list of failed test names); `CargoTestHandler`/`filter_cargo_test` detect the second occurrence and drop every line until `test result:` reappears, exactly matching Rust's `in_failure_names` state machine. (3) **`cargo nextest` discards everything after the `Summary` line** — post-summary `FAIL` recaps and `error: test run failed` are pure noise in the real tool's output; `filter_cargo_nextest`'s `past_summary` flag (ported as `pastSummary`) drops them, verified by dedicated tests asserting exactly one `FAIL [` header appears per failed test (not duplicated by the post-summary recap). (4) Clap's `trailing_var_arg = true, allow_hyphen_values = true` on all six subcommands means there is genuinely no clap-equivalent argument rejection to reproduce — verified empirically (not assumed) by inspection of the six `CargoCommands` variants; the only zero-args case (`rtk cargo` with no subcommand at all) follows the established `DotnetCommand` precedent (`"cargo: no subcommand specified"` + exit 1) rather than inventing a new convention, and no `CommandArgumentParseException`/outer-catch guard was needed since this module never throws it. Nothing was silently "improved": all caps (`CAP_ERRORS=20`, `CAP_WARNINGS=10`, `CAP_LIST=20`) and tee-hint behaviors (`Tee.ForceTeeHint`/`Tee.ForceTeeTailHint` for overflow, `Tee.TeeAndHint` for the streamed-subcommand post-run hint) match Rust's `src/core/truncate.rs` constants and `core::tee` functions exactly. Test coverage: 58 new tests in `RtkSharp.Tests/Commands/Rust/CargoCommandTests.cs` (39 pure-filter-function ports of Rust's `#[cfg(test)]` suite — build/test/clippy/install/nextest filters, `format_crate_info`, the 6 streaming-handler tests, and 6 `restore_double_dash`-with-raw scenarios exercised through the shared `GitCommand` helper — plus new dispatch-level coverage for the C#-only `CargoCommand.RunAsync` entry point, including an explicit end-to-end `--` double-dash-restoration assertion). Full suite: 2230 passed, 1 pre-existing Unix-only skip, 0 failures. |
| 25 "Port after core" commands ported in one batch: Python (`ruff`/`pytest`/`mypy`/`pip`), Ruby (`rake`/`rubocop`/`rspec`), JVM (`gradlew`/`mvn`), Go (`go`/`golangci-lint`), Graphite (`gt`), cloud CLIs (`aws`/`kubectl`/`oc`/`glab`), JS/system (`next`/`prettier`/`format`), small utilities (`json`/`deps`/`diff`/`summary`), and analytics/discovery (`smart`/`cc-economics`/`discover`/`learn`) | Respective `src/cmds/python/*.rs`, `src/cmds/ruby/*.rs`, `src/cmds/jvm/*.rs`, `src/cmds/go/*.rs`, `src/cmds/git/gt_cmd.rs`, `src/cmds/cloud/aws_cmd.rs`+`container.rs`, `src/cmds/git/glab_cmd.rs`, `src/cmds/js/{next,prettier}_cmd.rs`, `src/cmds/system/format_cmd.rs`, `src/cmds/system/{json_cmd,deps,summary}.rs`, `src/cmds/git/diff_cmd.rs`, `src/cmds/system/local_llm.rs`, `src/analytics/cc_economics.rs`, `src/discover/`, `src/learn/` | New ecosystem folders `RtkSharp/Commands/{Python,Ruby,Jvm,Go}/`, new siblings in the existing `Commands/Cloud/` (`AwsCommand.cs`, `KubectlCommand.cs`, `OcCommand.cs`, `ContainerFilters.cs` — Kubectl/Oc share the same Rust module as the already-ported `Docker`, so their C# port shares `ContainerFilters.cs` rather than duplicating Docker's logic), `Commands/Git/` (`GtCommand.cs`, `GlabCommand.cs`, `DiffCommand.cs`), `Commands/Js/` (`NextCommand.cs`, `PrettierCommand.cs`), `Commands/System/` (`FormatCommand.cs`, `JsonCommand.cs`, `DepsCommand.cs`, `SummaryCommand.cs`, `SmartCommand.cs`), `Commands/Analytics/` (`CcEconomicsCommand.cs`), and new top-level `RtkSharp/Discover/`/`RtkSharp/Learn/` modules (mirroring Rust's own top-level `src/discover/`/`src/learn/` placement, not nested under `cmds/`). All registered in `RtkSharp/Cli/CommandRegistry.cs`, classified against Rust's own `RTK_META_COMMANDS`/`PASSTHROUGH` split (`json`/`deps`/`smart`/`cc-economics`/`discover`/`learn` are `RTK_META_COMMANDS`; everything else in this batch is `PASSTHROUGH`). | Ported by 8 parallel subagents, each isolated to its own git worktree and constrained to add only new files (no shared/registry edits), following the same discipline as this project's earlier 9-language AST-analyzer port and reviewed/verified by the orchestrating session afterward — dispatch wiring, worktree consolidation, and final build/test verification were done centrally, not by any individual subagent. 838 new tests added (Python 40, Ruby 65, JVM 124, Go/Gt 91, cloud CLIs 210, JS/system 23, utilities 102, analytics/discovery 183). Full suite after consolidation and dispatch wiring: 3105 passed, 1 pre-existing Unix-only skip, 0 failures, 0 new build warnings. Smoke-tested end-to-end through the real CLI dispatch path (`rtk json`, `rtk smart --help`, `rtk diff <file> <file>`) to confirm registry wiring actually reaches each new handler, not just that the code compiles. Notable disclosed quirks preserved verbatim across this batch: `cargo`-style `--` restoration does not apply to `go test`/`build`/`vet` (Rust's own `go_cmd.rs` never calls `restore_double_dash` despite `trailing_var_arg`); `go test`'s filter parses `-json` NDJSON events directly rather than text block-grouping; `rtk diff <one-file>` silently discards that argument and reads a unified diff from stdin instead (Rust's own dispatch only branches on whether a second file was supplied); Maven's Surefire "trail re-arm" (a capped failure class drops every per-test sub-block, not just the first) and the `mvn -q` zero-byte-on-success behavior; RuboCop/Rspec's `--format json` auto-injection is skipped when the user already passed any format-flag variant, including combined short forms like `-fjson`. Individual per-command Rust-quirk detail is intentionally not fully enumerated row-by-row here given the batch size — see each new file's own class/method remarks for command-specific fidelity notes. |

## Verification

See Step 4 output in `.superpowers/sdd/task-5-report.md` for the full file
listing and spot-check confirmation that every file under
`tests/parity/fixtures/` corresponds to a command actually run during this
task (no fabricated content).
