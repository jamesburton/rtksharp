# `rtk pipe`/`rtk proxy`/`rtk run`/`rtk err`/`rtk test` Oracle Parity Report

> Auto-generated by `RtkSharp.ParityTests/PipeProxyRunParityTests.cs` (Phase 6 Task 7 acceptance gate). Do not edit by hand — re-run the test to refresh.

- **Generated (UTC):** 2026-07-05 07:59:45
- **Platform:** Windows
- **Oracle:** `C:\Development\rtksharp\target\release\rtk.exe`
- **RtkSharp:** `C:\Development\rtksharp\RtkSharp.ParityTests\bin\Debug\net10.0\RtkSharp.exe`
- **Battery size:** 22 entries (pipe passthrough/aliases/auto-detect/unknown-filter/oversized-stdin, proxy simple/quote-split/multi-arg/empty-args/exit-code, run -c/positional/empty/exit-code, err error/no-error, test cargo/pytest)

Comparison method: each entry runs both binaries with `RTK_DB_PATH` pointed at a fresh per-side temp file, `CLAUDE_CONFIG_DIR` redirected to a non-existent per-entry path (so `hook_check::status()`/`HookCheck.Status()` resolve deterministically to `Ok`, matching `GainParityTests`' established convention), and `RTK_TEE=0` (disables `err`/`test`'s tee-to-disk write). Every entry except `pipe`'s own stdin-driven scenarios passes `StdinContent = ""` (forced non-interactive stdin); `pipe` entries pass their scenario's actual stdin payload. Stdout (CRLF/LF normalized, trailing-whitespace trimmed) and exit code are compared; stderr is not (project convention — see `GainParityTests`/`HookParityTests`). No `HostRealDirGuard`/junction machinery is used: both empirical canaries (a real `rtk proxy` run and a real `rtk err` run against the actual oracle, with only the three env overrides above set) left the real `%LOCALAPPDATA%\rtk` directory's contents and timestamps completely unchanged — see the class remarks in `PipeProxyRunParityTests.cs` for the full empirical detail.

## Summary

| Metric | Result |
|--------|--------|
| **Overall parity (headline)** | **95.5%** |
| Threshold | 95% |
| Entries matched | 21 / 22 |

## Per-entry results

| Entry | Rust exit | .NET exit | Stdout match | Verdict |
|-------|-----------|-----------|--------------|---------|
| `pipe --passthrough` | 0 | 0 | yes | MATCH |
| `pipe -f git-log (genuinely delegating alias)` | 0 | 0 | yes | MATCH |
| `pipe -f cargo-test (disclosed-gap identity passthrough alias)` | 0 | 0 | no | MISMATCH (stdout) |
| `pipe -f grep (mini-filter, grouped/capped)` | 0 | 0 | yes | MATCH |
| `pipe -f find (mini-filter, grouped/capped)` | 0 | 0 | yes | MATCH |
| `pipe unknown filter -> error` | 1 | 1 | yes | MATCH |
| `pipe auto-detect: grep-line shape` | 0 | 0 | yes | MATCH |
| `pipe auto-detect: path-listing shape` | 0 | 0 | yes | MATCH |
| `pipe oversized stdin -> hard bail` | 1 | 1 | yes | MATCH |
| `proxy simple command (single arg, no spaces, no resplit)` | 0 | 0 | yes | MATCH |
| `proxy single-raw-arg quote-split (issue #388)` | 0 | 0 | yes | MATCH |
| `proxy multi-arg direct (no resplit)` | 0 | 0 | yes | MATCH |
| `proxy empty args -> error` | 1 | 1 | yes | MATCH |
| `proxy exit-code propagation (failing wrapped command)` | 3 | 3 | yes | MATCH |
| `run -c form` | 0 | 0 | yes | MATCH |
| `run positional-args form` | 0 | 0 | yes | MATCH |
| `run empty command -> no-op` | 0 | 0 | yes | MATCH |
| `run exit-code propagation` | 7 | 7 | yes | MATCH |
| `err: synthetic command producing a Rust-style error signature` | 0 | 0 | yes | MATCH |
| `err: synthetic command with no errors` | 0 | 0 | yes | MATCH |
| `test: synthetic cargo-test-shaped output` | 0 | 0 | yes | MATCH |
| `test: synthetic pytest-shaped output` | 0 | 0 | yes | MATCH |

## Mismatch details

### `pipe -f cargo-test (disclosed-gap identity passthrough alias)`

- Verdict: **MISMATCH (stdout)**
- Rust exit: `0`, .NET exit: `0`
- Rust stdout: `cargo test: 3 passed (1 suite)`
- .NET stdout: `test result: ok. 3 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`

